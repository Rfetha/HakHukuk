# Karar kaydı — ADR-0064 … 0086

> **Ne bu:** `v1.0` kapısı, hakem paneli, ürün yolu ve yayın turunun kararları —
> **birleştirilmiş ve damıtılmış**. Tekil dosyalar 2026-09-13'te silindi ([ADR-0086](#adr-0086));
> **sayılar birebir korundu**, silinenler `301a82f` ve öncesinde duruyor.
> Her ADR bir çapa taşır → `kararlar-0064-0085.md#adr-0074`.

| # | statü | konu |
| :-- | :--: | :--- |
| [0064](#adr-0064) | 🟢 | **`v1.0` kapısı: üç maddeli, δ = 2,0 puan** — ön-kayıtlı formül, mekanik sayı |
| [0065](#adr-0065) | 🟢 | Bölünmüş sürümleme: ürün sürümü ↔ iddia sürümü |
| [0066](#adr-0066) | 🟢 | B1 yöntemi: reddetme-örneklemesi; GRPO bütçe kapısıyla ertelendi |
| [0067](#adr-0067) | 🟢 | DEV/TEST `core_hard` soru onarımı (v1 → v2) |
| [0068](#adr-0068) | 🟢 | `RRF_K` 60 → 10 (hibrit füzyon) |
| [0069](#adr-0069) | 🟢 | Kabul testi raporlaması: ham kütle + tavan kullanımı |
| [0070](#adr-0070) | 🟢 | Üretim bütçesi rakiple EŞİTLENDİ (tek formül, 1536) |
| [0071](#adr-0071) | 🟢 | `v1.0` release artefaktı: tek GGUF, adında kuantizasyon |
| [0072](#adr-0072) | 🟡 | `v1.0` rakip havuzu genişler · ön koşulu hakem paneli |
| [0073](#adr-0073) | 🟢 | M5 rejimine DRY eklendi · kapsam yalnız M5 |
| [0074](#adr-0074) | 🟢 | **Hakem paneli kuruldu (iki aile), bağlayıcı hüküm kuralı** |
| [0075](#adr-0075) | 🟢 | `v1` SFT ile kapanır; `v2` = `tgta_v1` üstüne sequential RL |
| [0076](#adr-0076) | 🟢 | KAPI ↔ KALDIRAÇ ayrımı ve araç katmanı |
| [0077](#adr-0077) | 🔴 | Kabul testi koştu; `v1.0` VERİLMEDİ, sürüm `v0.3` *(→ 0084 ile aşıldı)* |
| [0078](#adr-0078) | 🟢 | Konteyner dağıtımı bir **REJİM KİLİDİDİR**, kolaylık değil |
| [0079](#adr-0079) | 🟢 | Zayıf-eşleşme rozeti EKLENMEZ |
| [0080](#adr-0080) | 🟢 | Ürün yoluna iki geçişli **zorunlu düşünce kapatması** eklendi |
| [0081](#adr-0081) | 🟢 | Altıncı `Durum`: `BOS_SORGU` |
| [0082](#adr-0082) | 🟢 | `app` kutusu da `--host 0.0.0.0` kullanır |
| [0083](#adr-0083) | 🟢 | **Kusur sicili ADR'ye taşındı** (`docs/superpowers/` boşaltıldı) |
| [0084](#adr-0084) | 🟢 | **κ borcu kapandı; kapının üç maddesi yeni birimde geçti → `v1.0`** |
| [0085](#adr-0085) | 🟢 | Tek model kartı, ürün tonu, iki yeniden adlandırma |
| [0086](#adr-0086) | 🟢 | **Kayıt katmanı kompaktlandı** — 103 dosya → 12 |

---

<a id="adr-0064"></a>
## ADR-0064 — `v1.0` kapısı: üç maddeli, δ = 2,0 puan · ön-kayıtlı formül, mekanik sayı

**🟢 Yürürlükte · 2026-09-06 (formül) / 2026-09-07 (sayılar)** · Karar: insan

**Ön-kayıt sırası — kural gereği.** **Formül 2026-09-06 öğleden önce**, hiçbir rakip sayısı görülmeden yazıldı; çıpa (`3.5 Flash`) o an **hiç ölçülmemişti**. Sayılar 2026-09-07'de **mekanik olarak** türedi. ⇒ Eşik sonuçtan sonra oynatılamaz.

```
(1) kütle ≥ (3.5 Flash'ın kütlesi) − 2,0 puan   ← ASIL KAPI, eşleşmiş rejim, aynı 80 soru
(2) isabetsizlik GERİLEMEZ                       ← vatandaş için en tehlikeli kusur sınıfı
(3) M5 (kör/parametrik) YÜKSELMEZ                ← ANTİ-HEDEF, ezber kazancı kapıdan geçmez
(*) her sayım adımında GÖZLE OKUMA zorunlu       ← sayısal kapı bozuk ölçümü bir kez geçirdi
```

**δ = 2,0 puan — neden bu sayı.** ⚠️ **Gürültü tabanına dayalı bir δ KURULAMAZ:** hakem tabanı (0,3 p) yalnız **A1** için ölçüldü; kütle = coverage × A1 ve **coverage'ın varyansı o tabanda yok**. δ ölçümden değil **insan kararından** gelir.

**Bağlayıcı okuma: GÖZ-katı (insan kararı).** Çekinme dedektörü rakip şablonlarında **fazla red sayıyor** (28 rakip kalemi gözle okundu: 3.1 FL'de 3, 3.5 FL'de 2, Flash'ta 1 **açık yanlış pozitif** + 5 çekinceli cevap; bizim kolda **0**). Üç okuma da raporlanır, **bağlayıcı olan en muhafazakârı** — rakiplerin çekinceli cevapları da **cevap** sayılır → onların kütlesi en yüksek, farkımız en dar. ⇒ ***"Kendi lehine okudun"* denemez.**

**Madde (1) — ✅ GEÇTİ**

| okuma | 3.5 Flash | eşik = Flash − 0,020 | BİZ | hüküm | fark |
| :--- | ---: | ---: | ---: | :--- | ---: |
| ALET (ham) | 0,6925 | 0,6725 | 0,8011 | ✅ | +10,86 p |
| GÖZ-orta | 0,7050 | 0,6850 | 0,8011 | ✅ | +9,61 p |
| **GÖZ-katı — BAĞLAYICI** | **0,7425** | **0,7225** | **0,8011** | ✅ **GEÇTİ** | **+5,86 p** |

**Eşit sınav kanıtlandı, varsayılmadı:** `recall@10` dört öznede de **0,9500**; `context_shown` **80/80 bayt-bayt aynı**; istem, bütçe (1536) ve hakem yığını aynı.

**Madde (2) — çıpa YENİ BİRİMDE yeniden türetildi.** Spec'te *"≤ 7/80"* yazıyordu; o sayı **v1 soru setinden** geliyordu ve **hiç gözle sayılmamıştı**. v2 biriminde **ilk kez tam gözle** sayıldı: **8/80**. Karar: **çıpa 8/80'e yeniden çivilenir, kural "gerileme yok"** — *eşik gevşetilmiyor, birimi düzeltiliyor; `7/80` ile `8/80` aynı birimde değildir ve kıyaslanamaz.* Bağlayıcı olduğu yer **bir sonraki eğitim turudur**.

**Madde (3) — ✅ GEÇTİ.**
🚨 **Önce: bu maddenin ÇIPASI YOKTU.** Madde *"M5 ≤ **bugünkü**"* diye yazılmıştı ama `tgta_v1`'in M5'i **hiçbir birimde hiç ölçülmemişti** — *"bugünkü"* diye bir sayı olmadığı için madde **kendi kendine referans veriyordu** ve hiçbir hüküm üretemezdi. **Ön-kayıt kuralına uyduğu için de denetimden geçmişti.** Çıpa ADR-0039 §2'den okundu (**çıpa BASE'dir, rakip değil**) ve base aynı birimde yeniden koşuldu. → tuzak **2.17**.
**Rejim: DRY** — ilk koşu geçerlilik kapısından kaldı (kesik 5/80 = %6,2 > %5, hakem çağrılmadı); gözle okundu: **3 yozlaşmış tekrar** (×21 · ×19 · ×82) + 2 bütçe kesilmesi → [ADR-0073](#adr-0073). ⛔ **Eşik oynatılmadı; değişen ALET.**

| kol | okuma | `coverage` | `A1` | **ezber kütlesi** |
| :--- | :--- | ---: | ---: | ---: |
| **BİZ** | ALET | 0,9500 | 0,4105 | **0,3899** |
| **BİZ** | **GÖZ** | 1,0000 | 0,4057 | **0,4057** |
| base | ALET | 0,9750 | 0,4818 | **0,4697** |
| base | **GÖZ** | 1,0000 | 0,4739 | **0,4739** |

⭐ **Hüküm dedektöre bağımlı DEĞİL** — kusuru iki kolu da aynı yönde etkiliyor; **ADR-0057'nin eşit sınavı burada koruyucu görev görüyor.**
**Gözle okuma — alet ÜÇÜNCÜ kez yanıldı.** Alet 6 kalemi çekinme saydı; göz **6/6'sını yanlış pozitif** buldu — altısı da hukuki bir **olumsuz hüküm** kuruyor **ve atıf yapıyor**, üstelik çoğu **yanlış** (base id 16: *"4711 Sayılı Türk Hakemlik Kanunu"* — **var olmayan bir kanun**). Mekanizma: `REJECT_RE`'nin *"bulunmamaktadır"* ailesi hukuk metninde iki iş görür — *"kaynakta yok"* (çekinme) ↔ *"kanunda böyle bir hüküm yok"* (**esasa ilişkin cevap**); **kör modda kaynak yoktur** ⇒ birinci okuma tanımı gereği imkânsız. ⇒ `suskunluk_terazisi`'nin **üçüncü** yanılması (14→8 · 11→7 · **6→0**), üçünde de **fazla red**. 🆕 Açık borç: `exact_reject`'in kör mod dalı — ⛔ **bugün düzeltilmedi**, *kapının sayısı üretildikten sonra aleti değiştirmek ADR-0050'nin yasakladığı hareketin sınırındadır.*

**Okuma.** Model **kaynaksızken base'den daha az isabetli** (A1 0,4057 ↔ 0,4739) ve gözle bakıldığında **iki kol da hiç susmuyor** (80/80). ⇒ **Kazanç ezberden gelmiyor.** *İstenen yön budur: "güncellik kütüphanede, ağırlıkta değil."*

**🟢 KAPI KAPANDI — üç madde de sayıyla · ⚠️ ama DEV'de.**
> 🚨 **Bu başlık `v1.0` demek DEĞİLDİR.** Eksik ikisi: (a) donmuş TEST'in **kabul testi koşmadı** (tavan ≈%75 ↔ DEV %95) · (b) her sayı hâlâ **tek hakem ailesinin** hükmü.

**Ne KURULMAZ.** ① *"TEST'te de geçeriz"* · ② *"hakem panelinden geçmiş bir hüküm"* (tek aile, κ yok, self-preference ölçülmedi) · ③ *"model bu kadar iyileşti"* — **kazancın büyük kısmı eğitimden değil ölçümden geldi** (soru onarımı · füzyon onarımı · bütçe eşitlenmesi); **model ağırlıkları hiç değişmedi.**

---

<a id="adr-0065"></a>
## ADR-0065 — Bölünmüş sürümleme: ürün sürümü ↔ iddia sürümü

**🟢 Yürürlükte · 2026-09-06** · Karar: insan (açık soru S1)

**Bağlam.** ARA KAPI düştü (merge M2b **0,766** ↔ eşik **0,8649**, 9,9 p altında) ve o kapı CP4-CP5 harcamasını yetkilendiren kapıydı — **o yetki yok.** Soru: kapı düşmüşken ürüne `v1.0` adı verilebilir mi?

**Karar — sürüm İKİYE ayrılır.** ÜRÜN sürümü `v0.2 → v1.0` (*"çalışıyor, indirilebilir, yayımlanan sayı yeniden üretilebilir"*) · İDDİA sürümü ARA KAPI'ya bağlı kalır.
**Gerekçe:** ARA KAPI bir **iddia** kapısıydı, ürün olgunluğunu ölçmüyordu ve ölçmek için tasarlanmamıştı. *İkisini tek sayıya bağlamak, ürünü yayımlamayı ölçmediği bir kapıya rehin verir; ayırmak her iki tarafı da dürüst tutar.*
**Ne değişmiyor:** ARA KAPI'nın düştüğü **her iki belgede de damgalı durur.** Gevşetilmiyor, yeniden tanımlanmıyor. **Düşmüş kapı düşmüş kalır.**

> 🚨 **AÇIKLIK ŞERHİ 2026-09-07.** *"`v1.0` = ADR-0064'ün kapısı geçilince verilir"* cümlesi tek başına yanıltıyordu: kapı **DEV'de** geçildi ve harfiyen okunursa *"bugün verilebilir"* çıkar. **Çıkmaz.** ⇒ **Doğru okuma: `v1.0`, kapı KABUL TESTİNDE geçilince verilir.** DEV'deki geçiş bir **ön koşuldur**, hüküm değil. ⚠️ İkinci ön koşul: her sayı hâlâ **tek hakem ailesinin** hükmü.

**Reddedilenler.** `v1.0` ver + ARA KAPI'yı yeniden tanımla (**eşiği geçemeyince eşiği değiştirmek**) · CP4-CP5 koşulana dek `v0.x`'te kal (~$19 ve **yetkisi yok**; ürünü ölçmediği bir kapıya rehin verir).

---

<a id="adr-0066"></a>
## ADR-0066 — B1 (isabetsizlik) yöntemi: reddetme-örneklemesi; GRPO/RLVR bütçe kapısıyla ertelendi

**🟢 Yürürlükte · 2026-09-06** · Karar: insan

**Borç.** Model **gerçek ama soruya uymayan** bir maddeden cevaplıyor. **Vatandaş ürünü için en tehlikeli kusur sınıfı:** cevap iyi biçimli, atıf **doğrulanıyor**, ama yanlış hükmü gösteriyor — *uydurulmuş atıftan (bizde 0/114) daha sinsi, çünkü deterministik atıf doğrulaması onu **yakalayamaz**.* Ölçülen: **8/80** (v2 birimi, gözle tam tarama).

**⭐ Neden otomatik vekil metrik YOK — ölçüldü.** 75 cevaplanan kalemin tamamı gözle tarandı:

| süzgeç | bulduğu | **kaçırdığı** |
| :--- | ---: | :--- |
| `faithfulness < 0,6` | 4 | 27 · 36 · 41 — faith **1,00 · 1,00 · 0,67** |
| altın madde atıflarda yok | 3 | 29 · 30 · 42 · 61 — cevap altını **başka yerde anıyor** |
| **gözle tam tarama** | **8** | — |

⇒ Yüksek `faithfulness` isabetsizliği **elemez** (hakem, modelin dayandığı **yanlış maddeye göre** tutarlı bulabilir) ve atıf listesi de elemez. **ADR-0055'in ekseninde kodun açılmamış olmasının mekanik açıklaması budur.** ⛔ **B1 turunun kabul ölçütü gözle okumaya dayanmak zorundadır.**

**Karar — ÖNCE reddetme-örneklemesi:** `n cevap üret → yalnız ATIFI ALTINA DOĞRULANANLARI tut → TRL SFTTrainer`. Alet zaten var ve doğrulanmış (`b10_hasat.py`, Modal `harvest_b10`, sızıntı süzgeci 13.350 → **12.914**). Tahmini **~$5**.
**⛔ GRPO/RLVR ertelendi — gerekçeli.** Verifier'larımız **deterministik ve programatik** ⇒ B1 **doğrulanabilir ödül** problemine birebir uyuyor ve **yöntem olarak daha doğru eşleşme budur.** **Ama** 8-16 rollout × (1024+512) → **$10-30 tahmin**, Modal'da **$29,19** var ve **tahmin ölçülmedi**. **Açılma koşulu (ön-kayıtlı):** reddetme-örneklemesi turu kapandıktan sonra isabetsizlik 8/80'in altına inmediyse GRPO **kendi ADR'si ve kendi bütçe kapısıyla** açılır; açılmadan önce **küçük bir pilotla birim maliyet ölçülür — tahminle bütçe harcanmaz.**

**Reddedilenler.** Doğrudan GRPO · ikisini de koş-karşılaştır · **istem katmanında çöz — reddedildi ve ÖLÇÜLDÜ:** önsöz isabetsizliği 7/80 → 5/80 yapıyordu ama kütleyi **4,6 puan** düşürüyordu. ***Yama, eksenin kendisini ödemez.***

---

<a id="adr-0067"></a>
## ADR-0067 — DEV/TEST `core_hard` soru onarımı (v1 → v2)

**🟢 Yürürlükte · 2026-09-06** · Karar: insan (15 önerinin tamamı onaylandı)

**Bağlam.** `recall@10`'un kaçırdığı 10 kalem gözle okundu: **kaybın yarısı erişim hatası değildi** — 5 kalemde soru altın maddeyi **belirlemiyordu**. Tarama 155 sorunun tamamına uygulandı; üç kusur sınıfı (**A** askıda gönderim · **B** kanun ailesi belirsiz · **C** kurum belirsiz) ve üçü de soruların **altın maddenin içinden üretildiğini** gösteriyor. En ağır örnek: İİK 31/a **gemi sicili** hakkındadır, sorusu *"Mahkeme benim lehime bir karar verirse ne olur?"* idi — **belirsiz değil, neredeyse ilgisiz.**
⭐ **Eşleşmeler tam metinle denetlendi ve DOĞRU çıktı** ⇒ kusur **yer-gerçeğinde değil**, sorunun bağlamının **sökülmüş olmasında**.

**Karar.** DEV **13 soru**, TEST **2 soru** yeniden yazıldı → v2. Eski dosyalar saklandı; altın alanlar ve `n` **birebir korundu**.
**Dokunulmayanlar, gerekçeli.** ⛔ `canon/trap.jsonl` (35 kalem) — oradaki tuhaf sorular kusur değil, reddetme davranışını ölçen **kasıtlı tuzak**; *aynı ölçütle "düzeltmek" testin kendisini yok ederdi.* ⛔ **TEST id 31** — soru maddenin ifadesini **birebir kopyalıyor**, yani erişimi **yapay kolaylaştırıyor**; *düzeltmek kendi sayımızı **yükseltirdi*** → dokunulmadı, damgalandı.

**Yanlılık korumaları.** ① her soru **yalnız altın maddenin metnine** bakılarak yazıldı — retriever çıktısına, model cevabına, hangi kalemin kaçırıldığına **bakılmadı** · ② ⛔ maddenin **ayırt edici ifadeleri kopyalanmadı** (kopyalamak `recall`'u yapay şişirir) · ③ 15 önerinin tamamı **insana liste hâlinde** sunuldu.

**Ölçülen sonuç.** `recall@10`: **0,8750 → 0,9375** · kaçan **10 → 5**. Kontrol grubu çıpayı **birebir** yeniden üretti. Kütle tavanı %87,5 → **%93,75**.
🚨 **Bir kayıp kayda geçti ve GERİ ALINMADI: id 79.** Yeni soru, eski sorunun sözcüksel çıpasını kaybetti ve kalem **bulunuyorken kaçtı**. *Geri almak, eval sorusunu ölçülen sistemin lehine ayarlamak olurdu — 2. korumanın doğrudan ihlali.*

**Bedeli — açıkça.** ⚠️ **`#39-#61` arasındaki her DEV sayısı v1 birimindedir** ve damgalanmalıdır: kütle (%68,4 · %73,0 · %62,8) · aşırı-red (9/80 · 8/80) · isabetsizlik (5/80 · 7/80) · **ARA KAPI eşiği 0,8649 ve merge M2b 0,766** · ADR-0052'nin sıçraması · B10'un gözle okuması. *Harcanan şey **kaydın karşılaştırılabilirliği**.*

**Reddedilenler.** Değiştirme-yalnız etiketle (sorular bozuk kalır) · yalnız 7 kesin kalemi düzelt (*aynı kusur sınıfı iki kez açılırsa kayıt iki kez kırılır*) · soruları insan yazsın.

---

<a id="adr-0068"></a>
## ADR-0068 — `RRF_K` 60 → 10 (hibrit füzyon)

**🟢 Yürürlükte · 2026-09-06** · Bedel **$0** — RRF sorgu anında uygulanır, indeks yeniden kurulmaz

**Bağlam — kusur BM25'te değil, FÜZYONDA.** Kalan 5 erişim hatasının **dördünde bir kol altını zaten mükemmel bulmuştu** (id 79 yoğun sıra **0**, id 12 BM25 sıra **0**); RRF ikisini toplayınca ilk 10'un dışına itti. Aritmetiği: `k=60`'ta `0. sıra + 97. sıra = 1/60 + 1/157 = 0,0231`, oysa **iki kolda da 5. sıra** olan bir kalem `2/65 = 0,0308` alıyor. ⇒ ***"İki kolda vasat olmak, bir kolda mükemmel olmayı yeniyor."*** `RRF_K=60` **çok-sistemli** füzyon için varsayılandır; kolları çok farklı davranan **iki kollu** bir sistemde bu bir **kusurdur**.

**Karar: `RRF_K = 10`. Seçim gerekçesi PLATO, sivrilik değil.**

| RRF_K | r@1 | r@5 | r@10 |
| ---: | ---: | ---: | ---: |
| 1 | 0,5625 | 0,8125 | 0,9250 |
| 5 | 0,5625 | **0,8250** | 0,9375 |
| **10** | 0,5500 | **0,8250** | **0,9500** |
| 20 | 0,5125 | **0,8250** | 0,9375 |
| 30 | 0,5250 | 0,8125 | 0,9375 |
| 60 | 0,5125 | 0,7875 | 0,9375 |
| 120 | 0,5125 | 0,7875 | 0,9250 |

`r@5 = 0,8250` **k∈[5,20] boyunca sabit** — sağlam olan bu plato; **k=10 platonun ORTASI.** ⚠️ `r@10`'daki fazladan kalem **BONUSTUR, seçim gerekçesi değildir** — tek başına alınsa **aşırı uydurma** olurdu.
⚠️ **Takas gizlenmiyor:** 12 · 72 · 79 kurtuluyor ama **7 ve 25 düşüyor.** Net −1 kaçak, **saf kazanç değil.**

**⭐ Genelleme donmuş TEST'te DOĞRULANDI (seçimden SONRA).** Peşinen taahhüt edildi ve tutuldu: **TEST'te kazanan değer seçilmedi** — ikisinde de kazanan zaten aynı çıktı. TEST core_hard v1 **0,7250 → 0,7500 (+2,5)** · v2 aynı · DEV v1 +1,25 · DEV v2 +1,25. **Dördünde de yükseliyor** ve kazanç TEST'te DEV'dekinin **iki katı** ⇒ DEV'e uydurulmuş bir seçim değil.

**🚨 Yan bulgu — TEST erişimde yapısal olarak ~15 puan daha zor.** `DEV v1 @k=60 = 0,8750` ↔ `TEST v1 @k=60 = 0,7250`, ve fark **soru onarımından ÖNCE de vardı**. ⇒ **v1 kabul testinin kütle tavanı ≈ %75'tir, DEV'in %93,75'i değil.** *Yayımdan önce bilinmeli; kabul testi sonucu DEV sayısıyla kıyaslanamaz.*

**Doğrulama.** `pytest` 112 passed, 2 xfailed. `retriever.py` docstring'indeki **eski çıpa (0,875) silinmedi, damgalandı.**
**Reddedilenler.** max-füzyon (`r@10` 0,9000) · yalnız yoğun kol (0,9000 — **hibrit hâlâ önde**) · k=20 (**platonun kenarı**).

---

<a id="adr-0069"></a>
## ADR-0069 — Kabul testi raporlaması: ham kütle + tavan kullanımı

**🟢 Yürürlükte · 2026-09-06** · Karar: insan · **Ön-kayıt: kabul testi henüz koşmadı**

**Bağlam.** İki setin **erişim tavanı farklı**: DEV (80) `recall@10` **0,9500** → kütle tavanı **%95** · TEST core_hard (40) **0,7500** → **%75**. Kütle `recall@10`'u **aşamaz**. Fark setin daha zor sorular içermesinden değil **bileşimden** geliyor: ayrım kanuna göre katmanlı ama **madde uzunluğuna göre katmanlanmamış**; en zor uzunluk diliminde (Q4, `recall@10` 0,6667) DEV'in payı %12, TEST'in **%50**. **Bileşim farkın %81'ini açıklıyor.**
**Risk:** DEV'de %73,0 yayımlanır, kabul testinde ~%58 çıkarsa, **model hiç kötüleşmemiş olduğu hâlde tablo gerileme gibi okunur.**

**Karar.** ① **Ham kütle MANŞET ve BAĞLAYICI sayıdır.** ② Yanına tek bir tanısal oran: `tavan kullanımı = kütle ÷ recall@10` — *"altın madde bağlama girdiğinde model onu ne kadar iyi kullanıyor"*; her seti **kendi tavanına** böldüğü için **setten bağımsızdır**. Bugünkü DEV: `0,730 ÷ 0,9375 = 0,779`. ③ **Ön-kayıtlı korumalar:** ⛔ tavan kullanımı **KAPI DEĞİLDİR** · ⛔ **rakip kıyas cümlesi bu orandan KURULMAZ** · ✅ her iki setin `recall@10`'u **zorunlu** olarak yanında yazılır · ✅ DEV ile TEST **aynı metrik değildir**, yan yana konurken tavan farkı damgalanır.

**Reddedilenler.** Yalnız ham + şerh (*"%73 duyurduk, %58 çıktı"* okumasını engellemiyor) · ayrımı yeniden katmanla (**donmuş TEST'i açar**) · tam "standardize kütle" (**zorluk düzeltmesi kötü sonucu süslemenin kapısıdır**; tavan kullanımı bundan dar — tek bölme, gizli ağırlıklandırma yok).
**Neden ŞİMDİ karar verildi.** Kabul testi **henüz koşmadı**; *sayı görüldükten sonra "zorluk düzeltilirse şu kadar" demek, kuralın engellemek için var olduğu şeydir.*

**Bu ADR'nin KAPATMADIĞI.** 🆕 **Uzun madde chunk'lama borcu** (Q4'te `recall@10 = 0,6667`) — chunk = tam madde kararının **ilk ölçülen bedeli** · ⚠️ ayrımın katmanlanmamış olması.

---

<a id="adr-0070"></a>
## ADR-0070 — Üretim bütçesi rakiple EŞİTLENDİ (tek formül, 1536)

**🟢 Yürürlükte · 2026-09-06** · Karar: insan · ADR-0043 §2 adım 1'i değiştirir

**Bulgu — ölçüldü, varsayılmadı.** ADR-0043 §3 *"bütün rakipler aynı bütçeyle koşar; sapma hata vermez, sadece **kıyası geçersiz kılar**"* diyordu. **Bu şart tutmuyordu:**

```
rakip  (reasoning_budget dolu):  max_tokens = reasoning_budget + max_new_tokens = 1536
BİZ    (think_budget dolu):      max_tokens = think_budget                      = 1024
```

Protokol birebir uygulanmıştı — **kod ADR'ye sadıktı** — ama ayrı 512 payı **yalnız `content` boş kalan** kalemlere veriliyordu; model `</think>`'i kendi kapatınca cevap **1024'ün kalanına** sığmak zorundaydı. Ölçüm: bizim kolda zorla kapatılmayan **75/80** kalemin tavanı **1024**, rakiplerinki **1532** (ve 3.1 FL'de **36/80**, 3.5 FL'de **18/80** kalem 1024'ü aşıyor). ⇒ **Sapma bizim aleyhimize.**
🚨 **Yayımlanmış bir hüküm çürüdü:** *"bizde ADR-0043 gereği ayrık bütçe var"* — ikinci yarısı yanlıştı; **bizde de düşünce cevabın payını yiyordu, üstelik daha küçük bir toplamdan.** Şerh o dosyada **üstü çizilerek damgalandı, silinmedi.**

**Karar — tek formül, iki tarafta da:** `max_tokens = (reasoning_budget or think_budget or 0) + max_new_tokens`. Bizim kol da **1536** alıyor; zorunlu kapatma mekanizması **aynen duruyor**, yalnız tetiklendiği nokta kayıyor. **ADR-0043 §3 aynen yürürlükte — bu ADR onu ihlal etmiyor, ihlali onarıyor.**

**Tetikleyen olay.** F0.2 kesiklik kapısında düştü (5/80 = %6,2); 5 kesikten **4'ü tam 1024'te** ve `forced_close=False`. **Kesiklik bir belirtiydi, sebep bütçe eşitsizliğiydi.** ⭐ Kapı doğru çalıştı: hakem parası harcanmadan durdurdu.
⚠️ **Neden şimdi patladı:** önsöz kalkınca model `</think>`'i daha sık kendi kapatıyor (zorla kapatma **21/80 → 5/80**) → ayrı 512 payını alan kalem sayısı düştü. *S14 kararı bu kusuru **yaratmadı, açığa çıkardı.***

**Bedeli.** Bizim üç kolumuzun sayıları eski rejimde kalır ve damgalanır · ✅ **rakip kolları yeniden koşulmaz** (zaten 1536'daydılar) · bizim kol yeniden koşulur (~35 dk, $0).
**Reddedilenler.** Kabul et ve damgala (ön-kayıtlı %5 kapısı çiğnenir **ve** eşitsizlik ayakta kalır) · rakipleri 1024'e indir (**rakip tarafında devasa kesiklik üretir**).

---

<a id="adr-0071"></a>
## ADR-0071 — `v1.0` release artefaktı: tek GGUF, adında kuantizasyon

**🟢 Yürürlükte · 2026-09-07** · Karar: insan

**Karar.** ① **Tek artefakt: adaptörleri merge edilmiş TEK GGUF.** ② Dosya adı dört şeyi taşır — model · boyut · sürüm · **kuantizasyon**: `HakHukuk-4B-v1.0-Q4_K_M.gguf`. ③ **İç ad korunur** (`tgta_v1-q4_k_m.gguf`); iki ad **ayrı iş görür** (iç = izlenebilirlik, dış = model kartı/yayın).
**Neden kuantizasyon adın içinde.** Kuantizasyon bir ayrıntı değil, **artefaktın kimliğidir** — hem sayıyı hem donanım gereksinimini değiştirir (Q4_K_M: dosya **2,59 GiB**, VRAM ctx 4.096'da **3,09 GiB**). *Adında kuantizasyon olmayan bir dosya, "benim kartıma sığar mı" sorusunu **indirmeden** cevaplatmaz.*

**Reddedilenler.** **Adaptör + merge script'i yayımla** — üç bedeli var: (a) kullanıcı **8,66 GiB base**'i ayrıca indirip merge koşmak zorunda kalır; (b) merge host RAM'de bf16 akışıyla çalışır, **vatandaşın makinesinde yeniden üretilebilirliği ölçülmedi**; (c) **yayımlanan sayı merge+kuantize edilmiş dosyada üretildi — dağıtılan şey ölçülen şey olmalı.** · **Birden çok kuantizasyon** — her kuantizasyon **ayrı bir artefakttır** ve eşit sınav gereği kendi kapı koşusunu ister; `v1.0` tek nokta yayımlar, **kuantizasyon eğrisi açık borç.** · Adda sürüm yok.

**Açık kalan.** ⚠️ **Kuantizasyon eğrisi ölçülmedi** — Q4_K_M'in Q5_K_M/Q8_0'a göre kütle kaybı **bilinmiyor**; ADR-0031 hassasiyeti seçti ama **kaybı ölçmedi**.

---

<a id="adr-0072"></a>
## ADR-0072 — `v1.0` rakip havuzu genişler · ön koşulu hakem panelidir

**🟡 Kısmen uygulandı · 2026-09-07** · Karar: insan

**Bağlam.** Ölçülen dört özne de **tek sağlayıcıdan**. İnsan kararı: *"`v1.0` release olacaksa diğer sağlayıcıların 3.5-Flash seviyesindeki modelleriyle de karşılaştırılsın."* Gerekçe ürün-yönlü: *"Gemini'yi geçtik"* **bir sağlayıcının** o günkü hattına dair bir cümledir.

**Karar.** ① Havuz genişler — **en az bir başka sağlayıcının** aynı sınıftaki modeli aynı eşit sınavda raporlanır. ② ⛔ **Kapı eşiği OYNAMAZ** — çıpa `3.5 Flash` kalır, δ = 2,0 sabit; yeni özneler **raporlanır, eşiği kurmaz.** *Yeni bir özne bizi geçerse bu **negatif bulgu olarak** yazılır, kapı yeniden tanımlanmaz.* ③ 🚨 **AİLE DIŞLAMASI ENGELİ — havuzun şekli buradan çıkıyor:** Claude sınıfı ✅ sorunsuz · **GPT sınıfı ❌ çakışır** (hakem `gpt-4o-mini`) ⇒ hakem değişir ⇒ **bugünkü dört sayı da yeniden koşulur.** *Bir GPT öznesi eklemek, tek öznenin değil **panelin** bedelidir.* ④ **Sıralama kilitlenir: hakem paneli → sonra havuz genişlemesi.** *Panel kurulmadan havuzu genişletmek, aynı şüpheli hakemle daha çok sayı üretmek olur.*

**✅ Açık kısım kapandı (insan kararı):** havuza **TEK `anthropic/claude-*-sonnet*` sınıfı özne**. Bakiye $6,60 · özne başı ~$0,35 · aile dışlaması ✅ sorunsuz. *Bir özne "başka sağlayıcıyla da kıyaslandı" cümlesini **kurar**; üç özne bunu daha güçlü yapmaz ama bakiyenin kalanını yer.*
⚠️ **GPT sınıfı DIŞARIDA kalıyor ve bu `v1.0` yayınında eksiklik olarak yazılır.** Sebebi **bütçe değil usul.**

**Reddedilenler.** Havuzu şimdi genişlet-paneli sonra kur (*sıra tersine çevrilince aynı para iki kez ödenir*) · hakemi başka tek aileye çevir (**κ'yı üretmez**; sorun hakem **seçimi** değil, panelin **yokluğu**) · havuzu Gemini ile sınırlı bırak.
**Hâlâ açık.** ⚠️ **Model sürümleri kayar** — *"3.5-Flash seviyesi"* bir **sınıf** tanımıdır, birebir eşleşmez; *"eşdeğer sınıf"* iddiası **açıkça bir yargı** olarak damgalanır, ölçüm olarak değil.

---

<a id="adr-0073"></a>
## ADR-0073 — M5'in ölçüm rejimine DRY eklendi · kapsam yalnız M5

**🟢 Yürürlükte · 2026-09-07** · Karar: insan
**Ön-kayıt şerhi:** bu ADR, DRY'li koşuların **üretimi sürerken** ve **hiçbir M5 puanı görülmeden** yazıldı.

**Bağlam — kapı kaldı ve reçetesi uymadı.** M5 koşusu geçerlilik kapısından kaldı (kesik **5/80 = %6,2**, hakem çağrılmadı — **kapı parayı korudu**). Beş kalem gözle okundu ve **deterministik** bir kuyruk-tekrarı dedektörüyle bağımsız doğrulandı; ikisi **aynı hükmü** verdi: bütçe kesilmesi **2** (id 27: düşünce 1536'nın tamamını yaktı, cevap **3 karakter**) · 🚨 **yozlaşmış tekrar 3** (ibare **×21 · ×19 · ×82**).
ADR-0040'ın *"MAXTOK büyüt"* reçetesi **üç kalemde ölçülmüş biçimde etkisizdir**: `temperature=0` açgözlü kod çözmede döngüye girmiş model **matematiksel olarak çıkamaz** — büyük bütçe **daha uzun bir döngü** üretir. *#42 aynı sınıfta bütçeyi 8× artırmış ve hiçbir şey değişmemişti.* ⇒ Ön-kayıtlı kural, **kapsamadığı bir kusur sınıfına çarptı.**

**Ölçüm — ablasyon, $0, hakem yok.** ✅ **Kontrol kapısı: 5/5 BAYT-BAYT aynı** — alt küme resmî koşuyu birebir üretti, dolayısıyla fark **kola** aittir.

| kol | kesik | döngü | ort token |
| :--- | ---: | ---: | ---: |
| `kontrol` *(bugünkü rejim)* | **5/5** | **3** | 1843 |
| **`dry`** `0.8 / 1.75 / 2` | **0/5** | **0** | 1553 |
| `rep11` `--repeat-penalty 1.1` | 1/5 | 0 | 1508 |

**Karar.** ① M5'in rejimi DRY içerir; **kapsam yalnız M5** (`m1·m4·m2·m2b·m3·h1·h2b` değişmez). ② İki kol da (bizim + base) aynı bayraklarla yeniden koşulur. ③ ⛔ **Kapı eşiği OYNAMADI — değişen ALET.**
**Neden kapsam yalnız M5 — teknik zorunluluk, tercih değil.** **DRY bir `llama.cpp` örnekleyicisidir; Gemini'ye uygulanamaz** ⇒ rakip içeren hiçbir modda eşitlenemez (aksi hâlde biz cezalı, rakip cezasız koşar). **M5 rakip içermez** ve çıpası **base**'dir; base de yerel `llama.cpp` ⇒ DRY **iki kola da eşit** uygulanabilir.
**Neden DRY, `repeat_penalty` değil.** `ceza = 0,8 × 1,75^(n−2)` — tekrarlanan **dizinin** uzunluğuyla üstel büyür ⇒ **2 token'a kadar tekrar serbest**; hukuk metninde *"madde"*, *"kanun"* ve kanun numaraları **meşru olarak** tekrarlar ve ceza almaz, 20+ token'lık döngü ezilir. ⭐ **Determinizm korunur** — iki ceza da logit'i **seçimden önce** değiştirir ⇒ `temperature=0` bozulmaz. *#42'nin yarısını kurtaran `temp 0.6` bunu bozardı; ucuz kaldıraç aynı zamanda metodolojik olarak temiz olandır.*

**🚨 Kabul edilen bedel — sayıdan ayrılamaz şerh. DRY modeli DOĞRU yapmadı, AKICI yaptı.** Kör modda cevaplar artık tam ve akıcı — **ve yanlış**: İş K. **31** → *"35. ve 36. Maddeler"* · İİK **79/a** → *"110. Madde"* · TBK **230** → *"6502 Sayılı Tüketici Kanunu"*. **M5'in ölçmek için var olduğu şey tam olarak budur.** ⇒ 🚨 **DRY'li M5, DRY'siz M5 ile AYNI BİRİMDE DEĞİLDİR**; eski M5 sayıları **kıyaslanamaz ve kıyas kurulmaz** · ✅ bugün koşulan **iki kol aynı birimdedir** ⇒ hüküm **bu iki kol arasında** kurulur · ⚠️ mutlak değer yayımlanırken *"DRY'li rejim"* damgası **zorunlu**.

**Reddedilenler.** DRY'yi tüm yerel modlara (**eşit sınav bozulur** ve yine eşitlenemez) · MAXTOK büyüt (**ölçülmüş biçimde etkisiz**) · `temperature > 0` (determinizmi bozar) · M5'i *"ölçülemiyor"* diye raporla (dürüst ama kapı maddesini **sayısız** bırakır; ölçülebilir bir yol **$0,08**'e varken ölçmemek tercih edilmedi) · `repeat_penalty 1.1` (**1 kesik bıraktı**).

---

<a id="adr-0074"></a>
## ADR-0074 — Hakem paneli kuruldu (iki aile), bağlayıcı hüküm kuralı

**🟢 Yürürlükte · 2026-09-07**
> ⛔ **BU KURAL KAPI KOŞUSUNDAN *ÖNCE* YAZILDI.** *Sayı görüldükten sonra "şu hakeme göre…" demek kuralın engellediği şeydir.*

**Sorun.** Bugüne kadar yayımlanan **her** sayı `openai/gpt-4o-mini`'nin **tek başına** hükmüydü; κ hesaplanmamış, öz-tercih ölçülmemişti.

**Ölçülen.** İkinci hakem ailesi (`anthropic/claude-sonnet-5`) **aynı 80 cevabı** yeniden puanladı; üretim yeniden koşulmadı — **yalnız hakem değişkeni oynadı.**

| eksen | ölçü | değer | eşik |
| :--- | :--- | ---: | :--- |
| `faithfulness` | Pearson r | 0,705 | — |
| `cit_precision` | Pearson r | 0,487 | — |
| `tam_sadık` | **Cohen's κ** | **0,534** | κ ≥ 0,6 makul ⇒ **ZAYIF** |
| `atıf_temiz` | **Cohen's κ** | **0,409** | **ZAYIF** |

Kayma **tek yönlü**: Anthropic 24/80 kalemde daha düşük, 8/80'de daha yüksek not verdi. A1 farkı **11,33 puan** = gürültü tabanının (0,3 p) **38 katı**. Manşete etkisi: kütle `gpt-4o-mini` **0,8011** ↔ `claude-sonnet-5` **0,6940**.

**Karar (a) — bağlayıcı hakem = `openai/gpt-4o-mini`.** **Gerekçe tercih değil, eşit sınavdır:** kapının **iki tarafı da** yalnız bu hakemle ölçüldü; Anthropic hakem yalnız **bizim** kolumuzu puanladı ⇒ `0,6940` **eşiti olmayan bir sınavın sonucudur** ve kapıya **sokulamaz**. *Bir sayıyı başka hakemin eşiğiyle kıyaslamak, kapıyı sahte biçimde devirir ya da sahte biçimde geçirir — yön fark etmez, ikisi de aynı kusurdur.*
**Karar (b) — κ eşiğin altında: ne olur.** Yayımlanan sayıyı **geçersiz kılmaz** ama **kırılgan** yapar ⇒ manşetin yanında şu şerh **zorunludur**: *"Bu sayı tek hakem ailesinin hükmüdür. İkinci bir aile aynı 80 cevabı puanladığında kütle %80,1 → %69,4 düşmüştür (κ = 0,534, eşiğin altında). İki tarafı aynı hakemle ölçen bir kıyas kurulmadığı için bu düşüş kapı hükmüne girmez, ama sayının hakem seçimine duyarlı olduğu ölçülmüştür."*
**Karar (c) — hükmün hangi kısmı kaç aileye dayanıyor.** **Hiçbiri üç aileye dayanmıyor** — panel **iki ailelidir**, ADR-0032'den **sapılmıştır** ve bu yayında eksiklik olarak yazılır. Sapmanın sebebi ölçüldü (bakiye **$3,45**, rakip kolunu ikinci hakemle puanlama **$2,81** = bakiyenin %81'i); **insan kararı: harcanmadı, proje önceliği çalışan uçtan uca ürün.** **Öz-tercih ÖLÇÜLMEDİ** ⇒ *"hakem kendi ailesini kayırmıyor"* cümlesi **kurulmaz.**

**Sonuçlar.** `v1.0` kapısı `gpt-4o-mini` ile koşulur · `0,6940` **açık borç** olarak durur, **yayımlanmaz** · **borcun kapanma koşulu tektir: `3.5 Flash` kolunun aynı ikinci hakemle puanlanması** *(→ [ADR-0084](#adr-0084) ile ödendi)*.
**Reddedilenler.** Anthropic hükmünü bağlayıcı yapmak · panel ortalaması (**aykırıyı gizler**) · üçüncü aileyi eklemek (kapı hükmünü **çözmez**) · `0,6940`'ı yayımlamak (**hakemi eşleşmemiş kıyas**).

---

<a id="adr-0075"></a>
## ADR-0075 — `v1` SFT ile kapanır; `v2` = `tgta_v1` üstüne sequential RL

**🟢 Yürürlükte · 2026-09-08** · Karar: insan

**Bağlam.** Planın Faz 4'ü iki eğitim turu öngörüyordu (G14 `B1` ~$5 · G15 `B4` ~$1,3). Aynı anda üç şey ölçülmüştü: `B1`'de **rakiplerden geride değiliz** (biz 8/80 ↔ 8·7·8) · `B1` için **otomatik vekil metrik YOK** · `B4` — `τ_a` merge'de seyreliyor (0,987 → 0,766, −22,1 p). Ve bu hattın **iki turu** *"daha fazla eğitim"* varsayımıyla başlayıp **eğitimsiz kapanmıştı** (B10 · Faz 0: kütle %68,4 → %80,1, **ağırlıklar hiç değişmeden**).

**Karar 1 — G14 ve G15 koşulmaz; `v1` bugünkü SFT artefaktıyla kapanır.** *G14 zayıf bahis:* hedeflenen eksende geride değiliz ve ilerlemeyi ölçmek her turda **80 kalem gözle okuma** ister — bedeli para değil, **insan saati**. *G15'in hedefi `v2`'de KONUSUZ kalıyor:* B4 bir **merge** kaybıdır, eğitim kalitesi sorunu değil (`τ_a` tek başına 0,987); sequential mimaride merge yoktur ⇒ **aynı 22,1 puan ödenmeden geri gelir.** *G15'i koşmak, birkaç hafta sonra terk edilecek bir mimariyi onarmak olurdu.*

**Karar 2 — `v2` = `tgta_v1` yeni başlangıç noktası + GRPO / thinking RL.** **Neden RL, neden şimdi mümkün:** bu hatta **doğrulanabilir ödül** hazır — atıf doğrulaması **deterministik** (evet/hayır). Reward model eğitmeye gerek yok, LLM-hakem bedeli yok. Uydurulmuş madde **ölçülebilir bir negatif sinyaldir** (**0/114** ↔ rakipler 1·4·4).

**Karar 3 — 🚨 ADR-0027'nin task-vector hattı `v1`'de DONDURULUR — sessizce değil, burada.** `τ = θ_ft − θ_base` **tüm kolların aynı `θ_base`'i paylaşmasını** şart koşar; `tgta_v1`'i yeni başlangıç almak bu tanımı bozar ⇒ sonraki tur **task vector değil, sequential post-training** üretir. ⛔ *Bu bir kural ihlali **değil**, bilinçli bir mimari geçiştir — ama ADR-0027 hâlâ yürürlükteymiş gibi okunamaz.* **Merge iddiası `v1`'de ölçülmüş hâliyle kapanır ve `v2`'ye taşınmaz.**

**Karar 4 — ödül fonksiyonu çekinmeyi korumak zorundadır, pazarlık konusu değil.** ADR-0010 ölçtü: düz SFT abstention'ı **yok etti**; RL'de risk **daha keskindir** çünkü model ödülü maksimize etmeyi öğrenir.
```
r = doğrulanmış_atıf_oranı − uydurulmuş_madde_cezası + doğru_susma_ödülü − aşırı_red_cezası
```
⛔ **Ön-kayıt şartı:** `v2` başlamadan önce ödül fonksiyonu, hedef bant ve **durma kuralı** bir ADR'ye yazılır. Korunacak taban: uydurulmuş madde **0/114** · aşırı-red **4/80** · kütle **0,8011**.

**Reddedilenler.** G14/G15'i koşmak · `v1`'i RL'den sonra yayımlamak (**çalışan bir ürünü sonucu belirsiz bir tur için aylarca bekletmek**) · PPO (reward model + kritik ağ) · DPO (referans model ister, **net üstünlüğü ölçülmedi**).
**Ne KURULMAZ.** ⛔ *"RL daha iyi sonuç verir"* — **ölçülmedi, bir hipotezdir.** *Bu hattın tahmin sicili bunu gerektiriyor: "istem katmanı tavanı düşük" · "harness A1'i kapatır" · "norm dengeleme şart" · "B10 eğitim ister" — **dördü de çürüdü**.* ⛔ *"G14/G15 değersizdi"* — **koşulmadılar**, ne verecekleri **bilinmiyor**; karar bir **öncelik** kararıdır, ölçüm sonucu değil.

---

<a id="adr-0076"></a>
## ADR-0076 — KAPI ↔ KALDIRAÇ ayrımı ve araç katmanı (`v1`'e dahil)

**🟢 Yürürlükte · 2026-09-08** · Karar: insan

**Bağlam.** Bugünkü akış **tek atış ve sabit**: `soru → retriever (HER ZAMAN) → 10 madde → model → cevap → terazi (HER ZAMAN)`. Model hiçbir şeye karar vermiyor.

**🚨 Ölçüm, bu ADR'nin İLK GEREKÇESİNİ ÇÜRÜTTÜ — düzeltilerek bırakılıyor.** Taslak *"isabetsizlik 8/80 — model iki maddeyi yan yana okuyabilseydi farkı görürdü"* diyordu. **Ölçüldü ve yanlış çıktı:**

| eksen | büyüklük | altın madde bağlamda mıydı | araç çözer mi |
| :--- | ---: | :--- | :--- |
| isabetsizlik | 8/80 | **8/8 EVET** — biri **1. sırada** | ❌ sorun erişim değil, **SEÇİM** |
| aşırı-red | 4/80 | **4/4 EVET** — üçü 1. sırada | ❌ model bakıyor, kullanmıyor |
| uydurulmuş madde | **0**/114 | — | ❌ çözülecek sorun yok |
| **recall kaybı** | **4/80** | ❌ hiç gelmedi | ✅ `ara` **deneyebilir** — tek somut hedef |

⇒ **Araçların eval setindeki toplam hedefi 4 kalemdir** ve başarısı **garanti değil**. İsabetsizlik ve aşırı-red **muhakeme** sorunlarıdır.
**Kararın dayandığı gerekçe ve sınırı.** İnsan kararı: beş araç da konur; gerekçe **eval kazancı değil, ürün yeteneğidir** (eval seti *"soru → altın madde"* biçimindedir ve *"TBK 217 nedir"*, *"bu madde yürürlükte mi"* gibi gerçek soruları **ölçmez**). ⚠️ **Bu gerekçe ÖLÇÜLMEMİŞTİR ve öyle damgalanır** — *bu hatta spekülatif gerekçe daha önce iki kez çürüdü.* ⇒ Araç katmanının kazancı **yayımlanan hiçbir sayıya eklenmez.**

**Karar 1 — iki sınıf ayrılır ve karıştırılmaz.** 🔒 **KAPI** = güvenlik kontrolü, **hiç kimse** karar vermez, koşulsuz çalışır (atıf doğrulama · mülga süzgeci · durum sınıflandırma) · 🔧 **KALDIRAÇ** = modelin gücünü artıran deterministik araç, **model** çağırır (`ara` · `madde_getir` · `madde_var_mi` · `kanun_bul` · `yururlukte_mi`).
🚨 **KAPI'lar TOOL YAPILAMAZ.** Bugünkü en güçlü sayı — **uydurulmuş madde 0/114** — `terazi.siniflandir()`'in **her cevapta koşulsuz** çalışmasından gelir. *Tool'a çevrilirse model onu çağırmayı unuttuğu an garanti buharlaşır; ve model unutur.* ⇒ KAPI'lar tool döngüsünün **DIŞINDA**, döngü bittikten **sonra** çalışır.
**Karar 2 — araçlar deterministiktir.** ⛔ **Hiçbir araç LLM çağırmaz.** ⛔ *"`madde_var_mi` B1'i çözer"* cümlesi **KURULMAZ** — B1 vakalarında altın **zaten bağlamdaydı** ve uydurma **0/114**; *araç var olmayan bir maddeyi yakalar, B1'de madde **gerçek**, sadece **yanlış**.*
**Karar 3 — 🚨 döngü SINIRLI ve sınıra dayanmak GÖRÜNÜR:** `Durum.ARAMA_TUKENDI`. *Neden `KESIK` değil:* `KESIK` = *"üretim bütçesi bitti, cümle yarım"*, `ARAMA_TUKENDI` = *"cümle tam ama dayanağı eksik olabilir"* — **kullanıcıya söyledikleri farklı.** Gerekçe: bu hattın en pahalı hata sınıfı *"hata vermeden yanlış"* ve **agentic akış tam olarak bu riski büyütür.**
**Karar 4 — ⛔ araç katmanı KAPI KOŞUSUNA GİRMEZ; sıra bağlayıcı:** `1. kabul testi (ARAÇSIZ) → v1.0 hükmü · 2. araç katmanı · 3. yayın`. *Kapıyı araçlı koşmak eşit sınav kuralını ihlal eder: bizim kol araç kullanır, rakip kullanamaz.*

**Kapsam.** CLI + TUI ✅ yeterli · araç katmanı ▶️ `v1`'e dahil · HTTP API/web ⛔ `v2` · araç kullanımının **eğitimi** ⛔ `v2`. ⚠️ `v1`'de araç kullanımı **istem katmanındadır**, eğitilmediği için güvenilirliği düşük olacaktır ve bu **beklenen** bir durumdur.
**Reddedilenler.** Atıf doğrulamayı tool yapmak · döngüyü sınırsız bırakmak · `ARAMA_TUKENDI` yerine `KESIK` · araç katmanını kapı koşusuna almak · HTTP API'yi `v1`'e almak.

---

<a id="adr-0077"></a>
## ADR-0077 — Kabul testi koştu; `v1.0` VERİLMEDİ, sürüm `v0.3`

**🔴 [ADR-0084](#adr-0084) ile aşıldı · 2026-09-09**
**Karar:** model (insan *"tercih yok"* diyerek devretti — ⚠️ bu bir insan onayı **değil**, delegasyondur ve **tersine çevrilebilir**)

**Bağlam.** Donmuş TEST (n=40) **2026-09-09'da açık insan onayıyla, tek kez açıldı.** Rejim DEV koşusunun her ekseninde birebir; araçsız. Geçerlilik kapısı geçildi (kesiklik %2,5).

| | TEST (40) | DEV (80) |
| :--- | ---: | ---: |
| **ham kütle** *(manşet)* | **0,5804** | 0,8011 |
| `recall@10` = tavan | 0,7500 | 0,9500 |
| tavan kullanımı | 0,7739 | 0,8433 |
| uydurulmuş madde no | **0/52** | 0/114 |
| `wrong_ref_rate_micro` | 0,2424 | 0,0769 |
| aşırı-red | 5/40 [0,055–0,261] | 4/80 [0,020–0,122] *(aralıklar örtüşüyor)* |

Düşüşün **%76'sı** setin erişim tavanından.

**Sorun: donmuş TEST için ön-kayıtlı sayısal eşik YOK.** Kapının çıpası **DEV**'de ölçüldü ve rakipler TEST'te **hiç koşmadı** ⇒ eşik **mekanik olarak türetilemez**; tavan kullanımı **kapı değildir**; planın kendi cümlesi yalnız *"geçerse `v1.0`"* diyor ve **"geçmek" tanımsız**. ⇒ *Sayı görüldükten sonra eşik yazmak, ADR-0050'nin engellemek için var olduğu şeydir.* **Bu ADR eşik yazmıyor.**

**Karar — `v1.0` VERİLMEZ, sürüm `v0.3`.** Gerekçe **ADR-0064'ün kendi metnidir**: (a) kabul testi **bugün kapandı** · **(b) AÇIK** — bağlayıcı hakem hâlâ **tek aile**, κ `tam_sadık` **0,534** / `atıf_temiz` **0,409**, aracın 0,6 eşiğinin **altında**.
⇒ ***"`v1.0`'ı bloke eden şey model değil, ölçüm aygıtıdır."*** *Bu cümle küçültme değil: aynı aygıt bugün de kendi kusurunu yakaladı.*

**Bu koşunun tek başına haklı çıkardığı şey.** Donmuş TEST **bir kusur yakaladı**: `atif_dogrula.py` çok anlamlı kanun adında adayları `sorted()` ile geziyordu; `İŞ KANUNU` için ilk aday **1475 (mülga)** ve altın **4857/111** 1. sırada olmasına rağmen **doğru cevap MÜLGA damgası yiyordu** — *vatandaşa giden rozet buna bağlı.* TDD ile onarıldı (`DOGRULANDI` 50→52, `MULGA` 2→0). ⛔ **Manşet değişmedi** ve **geçmiş yayımlanmış hiçbir sayı oynamadı.**

**Ne KURULMAZ.** ① *"Model TEST'te kaldı"* — **kalınacak bir eşik yoktu** · ② *"`v0.3` bir gerileme"* — ağırlıklar **hiç değişmedi** · ③ *"(b) kapanınca `v1.0` otomatik gelir"* — **gelmez**, o zaman kapının üç maddesi **yeni birimde yeniden okunur**.
> **2026-09-13 güncellemesi:** madde (b)'nin kapanma koşulu [ADR-0084](#adr-0084)'te karşılandı — κ **değişmedi** (hâlâ 0,534/0,409), ama kapının üç maddesi artık **iki** bağımsız hakem ailesi altında da GEÇTİ. Bu ADR'nin metni **değiştirilmedi.**

---

<a id="adr-0078"></a>
## ADR-0078 — Konteyner dağıtımı: paketleme bir REJİM KİLİDİDİR, kolaylık değil

**🟢 Yürürlükte · 2026-09-10** · Karar: insan · **Genişletildi 2026-09-11 → [ADR-0082](#adr-0082)**

**Bağlam — konteynerin gerekçesi ölçülmüş bir kusurdur.**

| KV önbelleği | durum | `sha256` | uzunluk |
| :--- | :--- | :--- | ---: |
| `q8_0` — bütün ölçümlerin yapıldığı | SUSKUNLUK | `79b6915a…` | 201 |
| varsayılan (fp16) | ÇEKİNCELİ | `e97a2b85…` | 622 |

Aynı soru, aynı kod, `--seed 3407`, sıcaklık 0. **Değişen tek etken sunucu bayrağıdır ve cevap değişmiştir.** Bugün modeli indiren kişi kendi bayraklarını seçer; bağlayıcı yapılandırma yalnızca **düz metin olarak** üç belgede yazılıdır ve **hiçbir şey onu zorlamaz.**
İkinci boşluk **artefakt kimliğidir**: yayımlanan `0,8011` **belirli bir dosyanın** sayısıdır (`sha256 755e15e9…`, **2.783.446.720 bayt**); *"en güncel modeli çek"* diyen bir kurulum imajı sessizce başka bir artefakta bağlar.
Üçüncüsü koddadır: `servis.py` çalışma anında `sys.path`'e `scripts/` ekler, oysa `pyproject.toml` *"`scripts/` bilerek DIŞARIDA"* der ⇒ **paket tek başına kurulduğunda retriever'ı bulamaz.**

**Karar — üç kutulu `compose`, iki daemon, iki imaj.** `hakhukuk-indir` (tek seferlik; pinlenmiş revizyonu çeker → **`sha256` KAPISI** → volume; tutmazsa **iki servis de hiç başlamaz**) · `llama` (GPU; bağlayıcı bayraklar compose'da **yazılı**) · `app` (CPU; yalnız `127.0.0.1`'e yayımlanır).
**Madde 1 — bağlayıcı bayraklar birebir yazılır:** `-ngl 99 · -fa on · --no-context-shift · --cache-type-k q8_0 · --cache-type-v q8_0 · -c 8192`. **Tek sapma:** `--host 127.0.0.1` → `0.0.0.0` (konteyner içinde loopback komşudan erişilemez); **erişim yüzeyi daralmaz da genişlemez** — `ports: "127.0.0.1:8080:8080"`.
**Madde 2 — bayrak kümesinin bir TESTİ olur** (`tests/test_konteyner.py`, **Docker kurulu olmadan koşar**). *Bağlayıcı yapılandırma düz metin olarak üç belgede duruyordu; hiçbiri kapı değildi.*
**Madde 3 — indirme bir KAPIDIR** (revizyon pinlenir, `latest` yok; `sha256` + bayt karşılaştırılır, tutmazsa **erken çıkılır**). *ADR-0026 tanımsız base için aynı hükmü kurmuştu.*
**Madde 4 — indeks iki yollu**; `v1` kapsam kararı dağıtım kararıdır, paketleme kararı değil.
**Madde 5 — imaj repo ağacını kopyalar; borç YAZILIR, gizlenmez.** Konteyner kusur 11'i **onarmaz**; *retriever'ı pakete taşımak yapısal bir değişikliktir ve kendi turunu hak eder.*

**Elenen.** Tek imaj iki süreç (GPU'suz makinede imajın yarısı ölü) · ağırlığı registry imajına gömmek (**yayımlanan artefaktın ikinci bir kopyasını yaratır**; *"gerçek artefakt hangisi"* sorusu doğar) · yalnız app konteyneri (**rejim kilidi kurulmaz** — *kolaylık için paketlemek, ölçülmüş kusuru paketlememektir*) · indirmeyi iki servise dağıtmak (**`sha256` kapısı iki yerde durur** ve sessizce ayrışır).
**Ne KURULMAZ.** ① **Konteyner yayımlanan `0,8011`'i ÜRETMEZ** — o **ölçüm hattının** sayısı, konteyner **ürün yolunu** paketler · ② ticket 1/3/4/6 çözülmez · ③ `v1.0` iddiası doğmaz · ④ **`S9` açılmaz** (API ve konteyner **yerel ve tek kullanıcıdır**).

---

<a id="adr-0079"></a>
## ADR-0079 — Zayıf-eşleşme rozeti EKLENMEZ: eşik ölçüme bağlanmıştı, ölçüm reddetti

**🟢 Yürürlükte · 2026-09-11** · Karar: **model** — ön-kayıtlı kural gereği (*"ayrışmıyorsa rozet yoktur"* işten **önce** yazılmıştı)

**Bağlam.** Kullanıcı iki soruda birbiriyle tamamen ilgisiz kaynak listeleri gördü; model doğru davranarak sustu ama arayüz *"getirilen kaynaklar sorunuzla ilgisiz"* bilgisini **vermiyordu**. Rozetin var olabilmesi bir **ön koşula** bağlandı: `recall@10`'un **kaçırdığı** kalemlerin skor dağılımı, **tutturduğu** kalemlerinkinden **ayrışmalıdır**.
**Elenen — eşiği ölçmeden koymak.** *"Top-1 skoru 0,13'ün altındaysa zayıf de"* = **uydurulmuş eşiktir** (tuzak 2.17 · ADR-0050). Bedeli teorik de değildi: **doğru getirilmiş sorularda rozet çıkarsa vatandaş doğru cevaba güvenmemeyi öğrenir; hiç çıkmazsa rozet zaten yoktur.**

**Ölçüm.** n=80 DEV, CPU, 65,7 s, **$0**, hakemsiz. **Rejim çıpası birebir tuttu** (`recall@10` 0,9500 ↔ çıpa 0,9500) ⇒ *ayrışmanın yokluğu rejim farkının değil, **verinin** hükmüdür.* Kaçırılan dört kalem: `5237/89` · `5237/103` · `6284/10` · `6098/99`.

| gösterge | kaçırılan (n=4) medyan | tutturulan (n=76) medyan | yanlış alarm |
| :--- | ---: | ---: | ---: |
| **`ortalama_skor`** *(en iyi aday)* | 0,09276 | 0,10163 | **23 / 76** |
| `top1_skor` | 0,13691 | 0,16783 | 53 / 76 |
| `marj_1_2` | 0,02084 | 0,02051 | 56 / 76 |
| `std_skor` | 0,01934 | 0,02861 | 59 / 76 |
| `yayilim` | 0,06546 | 0,09129 | 60 / 76 |
| `entropi` | 2,28165 | 2,26997 | 57 / 76 |

⭐ İki gösterge daha keskin bir şey söylüyor: `marj_1_2` ve `entropi`'de **kaçırılanların medyanı tutturulanlarınkinden DAHA İYİ** — ***retriever yanlış maddeyi getirirken de kendinden emin görünüyor.*** Skor dağılımı bu hata sınıfını **işaretlemiyor.**
**n=4 damgası — zorunlu.** *Tek bir kalemin konumu değişse "ayrışma" görüntüsü tamamen değişirdi* ⇒ hüküm dar kurulur ve *"kesin ayrışmıyor"* **denmez.**

**Karar — rozet EKLENMEZ.** Ürün kodunda hiçbir sayısal eşik yok (`grep` ile çivilendi). **Eşik ölçüme bağlanmıştı, ölçüm reddetti.** *Dört kaçak için yirmi üç yanlış alarm, vatandaşın doğru cevaba olan güvenini bir hata sınıfını yakalamayan bir işaret uğruna harcamaktır.* **Bu bir BULGUDUR, başarısızlık değil** — emsali ADR-0062.
**Ne KURULMAZ.** *"Zayıf eşleşme sinyali imkânsız"* denmez — dediği tam olarak *"bu altı gösterge, bu 80 kalemde, n=4 ile ayırmıyor"* · *"retriever iyi çalışıyor"* **denmez** (`recall@10` bir **tavandır**, kapı değil) · yayımlanan hiçbir sayı etkilenmez.
**Sonuç.** **Kusur 5a AÇIK KALIR** · ölçüm aleti duruyor ve **yeniden kullanılabilir** — kaçırılan-kalem havuzu büyürse (donmuş TEST'te `recall@10` 0,7500) aynı soru **daha güçlü n** ile sorulabilir ve **hüküm değişebilir.**

---

<a id="adr-0080"></a>
## ADR-0080 — Ürün yoluna iki geçişli zorunlu düşünce kapatması eklendi

**🟢 Yürürlükte · 2026-09-11** · **REJİM DEĞİŞİKLİĞİ** — model tarafından uygulanmadan önce **DUR-ve-SOR kapısından** geçirildi; **karar verilmeden kod yazılmadı.**

**Bağlam — ürün yolu, ADR-0040'ın kendi geçerlilik kapısını GEÇEMİYORDU.**

| | ürün yolu | ölçüm hattı |
| :--- | ---: | ---: |
| kesik veya boş cevap | **7/80 (%8,75)** | 4/80 (%5,0) |
| **tamamen boş metin** | **4/80** (id 7·64·65·66) | 0/80 |

**Sınıf: sonlanmama, kesilme DEĞİL.** Yedi kesik kalemin yedisi de ölçüm hattında `finish=stop` ile tamamlanmıştır ⇒ **sebep soruların zorluğu değildir**; ve [#42](../record/kronoloji-39-52.md#42) aynı sınıfta bütçeyi **8× artırmanın hiçbir şeyi değiştirmediğini** ölçmüştü. **Bütçe eklemek bu kusuru kapatmaz.**
**Elenen — ikisi de insana sunuldu.** (1) *"Şimdi karar verme"* (çıpa elde, mekanizma yazılı, beklemenin karşılığı yok). (2) *"Ekleme — ürün yolu kesikliği gizlemiyor"* — bu `servis.py` docstring'inde **yazılı duran** gerekçeydi ve ölçümle **yetersiz** çıktı: *kesikliği görünür kılıyordu ama **sonlanmamayı karşılamıyordu**; vatandaşa `Durum.KESIK` damgalı **boş bir metin** teslim etmek, kusuru damgalamaktır — gidermek değil.*

**Karar + mekanizma (koddan OKUNDU, hatırlanmadı).** ① 1. geçiş: `max_tokens = 1024 + 512 = 1536`, `temp 0,0`, `seed 3407`. ② **Ayrım noktası:** `content` **BOŞ** ve `reasoning_content` **DOLU** ise — ve yalnız o zaman — 2. geçiş; **normal kalemde fazladan istek yoktur.** ③ 2. geçiş: ham istem sunucunun **kök** `/apply-template` ucundan alınır, `ham_şablon + iz + "\n</think>\n\n"` → `/v1/completions`, `max_tokens=512` — *model `</think>`'in **ötesinde** başlatıldığı için cevabı yazmak zorunda kalır.* ④ Dönen `finish_reason` **2. geçişinkidir** ⇒ **kesiklik gizlenmez.**

**Ölçülen sonuç** (80 DEV, $0, 1.061 s; üç `verify:` şartı bağımsız **yeniden hesaplanarak** doğrulandı):

| eksen | öncesi | sonrası | hüküm |
| :--- | ---: | ---: | :--- |
| **tamamen boş metin** | 4/80 | **0/80** | **GEÇTİ** |
| kesik | 7/80 (%8,75) | **3/80 (%3,75)** | **ADR-0040 kapısı GEÇTİ** |
| `sha256` değişen cevap | — | **4/80** — tam da bozuk olan dördü; **76 kalem birebir aynı** | değişiklik **CERRAHİ** |
| getirilen kaynak değişen | — | **0/80** | muhafız tuttu |
| doğrulanamayan atıf | 10 | 10 | kıpırdamadı |

**Suskunluk kümesi — fark GİZLENMEDİ:** `[10,34,63]` → `[7,10,34,63,64]`; **giren 2** (ikisi de eskiden **tamamen boş** dönen kalemler, artık açıkça çekiniyor), **çıkan 0**; diğer iki bozuk kalem tam **CEVAP**'a döndü.

**Ne KURULMAZ.** Bu karar yayımlanan `0,8011`'i **DEĞİŞTİRMEZ** (o **ölçüm hattının** sayısı) · *"model iyileşti"* **demez** — dediği dardır: **ürün yolu artık boş cevap teslim etmiyor** · **kütle bu koşuda ÖLÇÜLMEDİ.**
**Kabul edilen bedeller.** **(a) B10'a +2 kalemlik yük** — kesik 7→3'e düşerken aşırı-red iki kalem ağırlaştı; *kesikliği kütleden düşürmenin karşılığı, çekinme eksenine yazılan borçtur.* **(b) Açık kusur 20 — 2. geçiş `reasoning_content` alanına BAĞIMLIDIR:** sunucunun `--reasoning-format` varsayılanı değişirse mekanizma **sessizce tek geçişe düşer** ve boş cevap kusuru **geri gelir**; **testle çivilenemiyor** çünkü bağımlılık sunucunun davranışındadır, kodun değil.

---

<a id="adr-0081"></a>
## ADR-0081 — Altıncı `Durum`: `BOS_SORGU` — boş sorgu, "kaynakta karşılık yok" DEĞİLDİR

**🟢 Yürürlükte · 2026-09-11** · Karar: insan — **tip düzeyi** değişiklik, kod yazılmadan karara bağlandı

**Bağlam — rozet ile gövde AYNI ŞEYİ SÖYLEMİYORDU.** Rozet *"🤐 SUSKUNLUK — kaynaklarda karşılık bulunamadı"* diyordu; gövde *"Soru boş görünüyor."* **Rozet *"aradım ve bulamadım"* der; o dalda hiç arama yapılmaz** (kapı `_getir`'den öncedir) ⇒ rozet **gerçekleşmemiş bir işlemi rapor ediyordu.** İkinci ve daha sessiz sonuç: **boş sorgular suskunluk sayımının içine bir yol açmıştı.**

**Niçin `Durum` kapalı bir küme — ve niçin bu değer eklemeyi yasaklamaz.** `Durum` bir `bool` değil, **çünkü dünya ikili değil** ve bunu iddia eden alet **üç kez** yanıldı (üçünde de **fazla** red):
```
2026-09-06  bizim şablon, önsözsüz    alet 14 → göz  8
2026-09-06  Gemini şablonu            alet 11 → göz  7
2026-09-07  kör mod, iki kol          alet  6 → göz  0   (6/6 yanlış pozitif)
```
Kapalı küme olmasının işlevi **her hâlin adının olması ve adsız bir hâlin başka bir adın altına saklanamamasıdır** — *kapalılık, yeni bir hâl ölçülünce ona yeni bir ad verilmesini yasaklamaz, tam tersine şart koşar.*

| durum | vatandaşa söylediği | vatandaşın yapması gereken |
| :--- | :--- | :--- |
| `SUSKUNLUK` | *"Aradım; **kaynaklarda karşılık yok.**"* | soru geçerli — başka kaynağa git |
| `BOS_SORGU` | *"**Soru yazılmadı**; arama YAPILMADI."* | **soruyu yaz** |

**Elenen — `SUSKUNLUK`'u yeniden kullanıp ROZETİ değiştirmek.** Çelişkiyi görünürde kapatırdı ama **bütün suskunluk kalemlerini yanlış etiketlerdi**. *Tek bir kusurlu kalem için doğru sınıflandırılmış onlarca kalemin etiketini bulanıklaştırmak, **ölçüm aletini bozma** sınıfındaki hatadır.* Ayrıca ayrımı **tip seviyesinde** kurmak, **metinde** anlatmaya çalışmaktan üstündür (POSD, *errors out of existence*).
**Karar.** Altıncı değer `BOS_SORGU`; beş yüzey güncellendi; **kapı teraziden ÖNCEDİR** — *`terazi` bir MODEL ÇIKTISINI sınıflandırır; "boş sorgu" diye bir model çıktısı yoktur.*
**Ne KURULMAZ.** **Bu değer ÖLÇÜM HATTINDA ÜRETİLMEZ** ⇒ **yayımlanmış hiçbir sayı etkilenmez** — bu bir **ürün yüzeyi** düzeltmesidir · `api.py` şeması **BÜYÜMEDİ ve büyümemeli** (HTTP'de boş sorgu `answer()`'a hiç ulaşmaz, `422` önce durur ⇒ dal **ölü** olurdu).

---

<a id="adr-0082"></a>
## ADR-0082 — `app` kutusu da `--host 0.0.0.0` kullanır: ikinci sapma, icra sırasında doğdu

**🟢 Yürürlükte · 2026-09-11** · Karar: insan — sapma **onaylandı** · ADR-0078 m.2'yi **genişletir**

**Bağlam.** `hakhukuk/api.py`: `HOST = "127.0.0.1"  # S9 açılmadı; buranın değişmesi ürünün vaadini değiştirir`. Bu arayüze bağlanan bir süreç konteyner içinde **host'tan erişilemez** (`ports` konteynerin `eth0`'ına proxy'ler, **loopback'ine değil**) ⇒ servis ayağa kalkar ama **hiç kimse ulaşamaz** — **hata vermeden başarısız olan bir yapılandırma.**
**Karar.** `app` kutusu `uvicorn … --host 0.0.0.0 --port 8000` çağırır. İki koruma: ① **`api.py` DEĞİŞTİRİLMEDİ** — vaat ve şerh **yerinde durur**, sapma yalnız *paketleme* katmanında · ② erişim yüzeyi **genişlemez** (`ports: "127.0.0.1:8000:8000"`). ⇒ **`S9` yine AÇILMADI.**
**Elenen — `api.py`'ye ortam değişkeni eklemek.** Ürün kodunu **yalnız paketleme kolaylığı için** değiştirirdi ve şerhi **tam da şerhin uyardığı biçimde** aşındırırdı: *bugün konteyner için eklenen bir değişken, yarın barındırma için kullanılır ve `S9` **kimse karar vermeden** açılmış olur.*
**Niçin kendi ADR'si var.** Bu madde **icra sırasında doğdu**, tasarım turunda değil. ***Kilitli bir karar kümesinin dışında kalan bir sapmayı sessizce uygulamak, kilidin kendisini anlamsız kılar — kilit ancak kapsamadığı şeyi görünür kıldığı sürece kilittir.*** ADR-0078'in metni **değiştirilmedi**; çelişki **iki yerde** damgalanır.
**Bedel.** Uçtan uca doğrulama (`docker compose up`) o gün **henüz koşmamıştı**; bu ADR onun **yerine geçmez**.

---

<a id="adr-0083"></a>
## ADR-0083 — Kapanan planın kusur sicili ADR'ye taşındı; `docs/superpowers/` boşaltıldı

**🟢 Yürürlükte · 2026-09-12** · Karar: insan
**Kaynak:** silinen `plans/2026-09-07-hp-hat-a-hat-b.md`'nin `AÇIK KUSURLAR` bölümü + `DEVİR` bloğu

**Bağlam.** Plan **115/115 kapandı**; deponun kuralı bağlayıcıdır (*"kapanan plan yeniden işletilmez; kapandığı gün **kayda dönüşür**"*). İçeriğinin çoğu zaten başka yerde durur; **yalnız iki şey başka hiçbir yerde yoktur:** ① **kusurların sicili** ② **devir tablosu**. *Kusur sicili **plandan bağımsız** bir kayıttır: bir sonraki turun **girdisidir**, geçmiş bir işin ilerleme çizelgesi değil.*
**Devir kuralı aynen yürürlükte:** ***kapanış anında açık kalan her kusur, adıyla bir sonraki plana devredilir; hiçbir yere devredilmemiş açık kusur varsa kapanış GEÇERSİZDİR.***

### (A) Kusur sicili

> 🔄 **CANLI SAYIM — `v1-son-iş` KAPANDI 2026-09-13, 71/71: 28 KAPANDI · 3 DEVREDİLDİ · 4 AÇIK (5a · 23 · 27 · 31).**
> Turda kapananlar: **24** (`b077f13`) · **29** (adım 6.3.1) · **33** (`cc12b6e`) · **25** (Adım 8) · **10** (Adım 9, HF **PUBLIC**).
> ⚠️ Kusur **24** bu sicilde 2026-09-12'ye kadar *"AÇIK"* göründü — kapanışı `00-IS-SIRASI`'ya işlenmiş, **buraya işlenmemişti.** **Ders: *kapanış iki yere birden yazılmazsa sicil sessizce bayatlar*** — ve sicil bir sonraki turun **girdisidir**.

| # | kusur | durum | nerede kapandı / nereye devredildi |
| :-- | :--- | :--- | :--- |
| 1 | ürün yolunda 4/80 cevap **tamamen boş** (sonlanmama) | KAPANDI 2026-09-11 | **0/80**, kesik %8,75→%3,75 ([ADR-0080](#adr-0080)). Şerh: B10'a +2 kalem, kusur **20** doğdu |
| 2 | KV ayarının 80 kalemdeki etkisi ölçülmedi | KAPANDI 2026-09-11 | hakemsiz: %81 bayt değişimi. Hakemli ($0,045): kütle **0,8011 → 0,7932**; tabanın açıkça üstündeki tek eksen `wrong_ref_rate` ve **2,0× kötüleşti** ⇒ **insan kararı: `q8_0` KALIR** |
| 3 | TUI soru sorulunca donuyor | KAPANDI 2026-09-11 | çağrı çalışan iş parçacığına alındı |
| 4 | boş sorgu reddedilmiyor, sabit gürültü kümesi dönüyor | KAPANDI 2026-09-11 | `servis.answer`'da **kapı**, döngü dışında |
| **5a** | zayıf eşleşme sinyali kullanıcıya verilmiyor | **AÇIK** | **ölçüldü, ayrışma YOK, rozet EKLENMEDİ** ([ADR-0079](#adr-0079)): 4 kaçık için **23 yanlış alarm**. **Devir → `v2`** |
| 5b | kapsam bildirimi yok | KAPANDI 2026-09-11 | **statik** satır, sınıflandırıcı **YOK** (*yanılan bir kapsam sınıflandırıcısı cevabı olan soruyu öldürür*) |
| **6** | `wrong_ref` frontier'ın **9,3×** gerisinde (0,0769 ↔ 0,0083) | **DEVREDİLDİ** | **`v2` · borç `B1`**. ⚠️ **Bu turda AĞIRLAŞTI:** fp16'da **2,0×** kötüleşiyor — `q8_0` kararının gerekçesi budur |
| 7 | duman koşusundan doğrusal maliyet tahmini kapı kurmuyor ($0,82 ↔ $1,1932) | KAPANDI | **kural yazıldı, kod değil**: tuzak **1.11** |
| 8 | arayüz açılışında yönlendirme yok | KAPANDI | — |
| 9 | TUI göz kapısı açık | KAPANDI | insan teyit etti; kapı çalışırken bir ürün kusuru yakaladı (`python -m hakhukuk.tui` hiçbir şey yapmıyordu) |
| **10** | commit'ler push edilmedi · HF deposu **özel** | ✅ **KAPANDI 2026-09-13** | `push` 2026-09-12 (62 commit). **HF görünürlüğü** 2026-09-13, insan onayı: `repo_info(...).private = False`, **token'sız** alt süreçte indirilip `sha256` bayt bayt eşleşti. ⛔ Depo adı sonradan değişti → [ADR-0085](#adr-0085) |
| **11** | `hakhukuk` paketi tek başına kurulamıyor | **DEVREDİLDİ** | **kendi turu** ([ADR-0078](#adr-0078) m.5). Kalıcı çare `madde_anahtar`+`atif_dogrula`+`score_abstention` üçlüsünün **pakete taşınması** |
| 12a | `##begin_quote##` işaretleri **vatandaşa gidiyor** | KAPANDI | **yalnız sunum katmanında** süzülür. ⛔ Ölçüm hattına **girmedi**: `score_register.py` aynı işareti bir **register göstergesi** sayıyor |
| **12b** | işaretlerin **kaynağı** eğitim verisidir | **DEVREDİLDİ** | **`v2` · borç `B11`** — düzeltmesi **yeniden eğitimdir** |
| 13 | `Atif.madde_no` ↔ `Kaynak.madde_no` biçimi tutmuyor | KAPANDI | türetilmiş alan; **gösterim** tutarsızlığı ayrı kalır ⇒ kusur **27** |
| **14** | `madde_sayisi` **ölü alan** | KAPANDI | **insan kararı: SİL** (YAGNI); `7d1af54` |
| **15** | kapsam satırı **yalnız TUI'de** | KAPANDI | CLI · TUI · HTTP üçü birden; üç yüzeyde **çalıştırılarak** görüldü |
| **16** | boş sorguda rozet ↔ gövde çelişik | KAPANDI | **tip düzeyi**: altıncı `Durum` ([ADR-0081](#adr-0081)) |
| **17** | `tui.py` `bicimle()`'yi çağırmıyor — **iki paralel sunum katmanı** | KAPANDI | Turdaki iki sızıntının **kök nedeni**. **BİRLEŞTİR** — rozet **parametre** (bool bayrak yok) |
| **18** | ⭐ **atıf doğrulayıcı kanun adını YANLIŞ kanuna çözüyor** ve `DOGRULANDI` basıyor | KAPANDI | Alet **manşet `0/114`'ü üretenin ta kendisi**. Parantezli adlı kanunlarda **7/16 → 0/16**. `0031976`. Yan etkisi kusur 22 |
| **19** | fp16'da model **istem yer tutucusunu harfiyen basıyor** | KAPANDI | sunumda değiştirildi; **silinmedi, dürüstçe işaretlendi** — *madde numarası vatandaşın doğrulayabileceği tek adrestir* |
| **20** | 2. geçiş `reasoning_content`'e bağımlı, **sessizce tek geçişe düşebilir** | KAPANDI, **iki yarısı da** | kod: **erken ve gürültülü** `RuntimeError`; `compose.yaml`'da `--reasoning-format deepseek` pinlendi + **mutasyon denetimi** |
| **21** | `araclar.py` **kendi ikinci ad indeksini** taşıyor — kusur 18'in onarımı ürün yüzeyine GEÇMEDİ | KAPANDI | Tuzak **2.18** kalıbı: *vatandaşa giden atıf doğrulaması ile yayımlanan sayıyı üreten doğrulama **AYRI**ydı.* Ayrışma gerçekti (892 adda 19 · parantezli 16'da 7 · çıpada **24/114**). Eşdeğerlik: 94 dosya · 11.634 hüküm, `sha256` **birebir** |
| **22** | rakip kollarının uydurma-madde sayıları **onarılmamış aletle** üretildi | KAPANDI | **YENİDEN PUANLA.** Sonuç **aleyhimize** ve **yazıldı**: `3.1 Flash-Lite` **`1/152` → `0/153`** |
| **23** | ilk yeniden-puanlama koşusu **eski sayıyı verdi ve tekrarlanamadı** | **AÇIK — ikinci kez sınandı** | Modül gölgeleme arandı, **bulunamadı**. Deterministik taraf üç bağımsız kez `sha256` birebir. **Devir → `v2`** |
| **24** | README'lerin *"kurup çalıştırabilir miyim? — HAYIR"* tablosu BAYAT | ✅ KAPANDI 2026-09-12 (`b077f13`) | *"`hakhukuk/` dizini yok"* ve *"ağırlıklar yayımlanmadı"* — **ikisi de YANLIŞtı** |
| **25** | `MODEL_CARD` uydurulmuş-madde satırında **iki farklı kesir birimi** | ✅ KAPANDI 2026-09-13 | **tüm paydalar `DOGRULANDI`.** Yeniden puanlama yok, aynı sayımın başka ifadesi; sayılar **oynamadı** |
| **26** | ⭐ sunumda **çift tırnak** (`“ "…" ”`) | KAPANDI | **İNSAN GÖZÜ KAPISINDA yakalandı**; kusuru **aynı gün biz eklemiştik** ve testler **tırnaksız** alıntıyla yazıldığı için görmemişti |
| **27** | Kaynaklar listesinde **madde biçimi tutmuyor** (`MADDE 349` ↔ `Madde 8`) | **AÇIK** | Aynı göz kapısında görüldü. Korpusun **ham** tutarsızlığı; veri bozulmasın diye ham alan korunuyor. **Devir → `v2`** |
| **28** | ⭐ `hakhukuk-*` komutları **ÇALIŞMIYOR** — üstelik **belgeler o komutu öneriyordu** | KAPANDI | İki sebep: paket venv'e **hiç kurulmamıştı**; ve **latent kusur** — `license` PEP 639 dize biçimi, `setuptools>=68` onu reddediyordu (`--no-build-isolation`'da patlıyordu). `setuptools>=77` + test **kapı** olarak çivilendi |
| **29** | imaja ürünün **okumadığı** 36,1 MiB korpus yedeği giriyor | KAPANDI 2026-09-12 | `.dockerignore`; **2,13 GB → 2,08 GB**. Aynı turda `docker compose up` **temiz volume'le** uçtan uca doğrulandı; üç `sha256` kapısı bağımsız doğrulandı, konteynerde 80 DEV sorusu koşuldu, boş cevap **0/80** |
| **30** | ⭐ `cli.py` künye yolunu **repo köküne göreli** çözüyor | KAPANDI | **İmajın İÇİNDE** ölçüldü. **Sessiz bozulma:** patlamıyor, vatandaş *"künye okunamadı"* okuyor. Kusuru **aynı tur biz ekledik**; host'ta görünmüyordu çünkü paket **editable** kurulu |
| **31** | `compose` volume adını **proje adından** türetiyor | **AÇIK (kayıt)** | **Kusur değil, kayda değer davranış:** hazırlıkta doldurulan volume **kullanılmadı** ⇒ **iyi ki öyle oldu** — artefakt elle konsaydı `sha256` kapısı **hiç ateşlenmeyecekti** |
| **32** | 🚨 **KONTEYNER UÇTAN UCA ÇALIŞMIYOR** — her soruda HTTP 500 | KAPANDI | Kök sebep bir **onarımın ters yüzü**; aşağıda |
| **33** | 🚨 `yeniden_uret.sh`'ın recall@10 kapısı **var olmayan alanı** okuyordu | KAPANDI 2026-09-12 | `recall@10` her koşuda **0,0000** okunuyor, kapı **hiçbir koşuda geçemezdi** — çıpanın kendi verisinde bile. TDD ile onarıldı; `cc12b6e` |

### (B) Taşıyıcı gerekçeler — tek cümleye sığmayan dört bulgu

**Kusur 18 — korunma ALETTEN değil, ÖRNEKLEMDEN geliyordu.** Onarımdan sonra manşet `0/114` ve TEST'in `0/52`'si **OYNAMADI**. **Bu bir aklanma DEĞİLDİR:** 114 atfın hiçbiri **parantezli adlı** bir kanuna denk gelmemişti; alet parantezli adlı **16 kanunun 7'sinde** yanlış kanuna `DOGRULANDI` basıyordu. ***Yayımlanmış sayı doğruydu — ama doğruluğunu aletin sağlamlığına değil, örneklemin rastlantısına borçluydu.*** *Bu ayrım yazılmazsa kayıt "alet kusurluydu ama sayı tuttu, demek ki kusur önemsizdi" diye okunur ve bu yanlıştır.*
⚠️ Yeniden **puanlamak** yeni sınav açmak değildir — **aynı cevaplara düzeltilmiş aleti uygulamaktır.**

**Kusur 22 — rakipler yeniden puanlandı; sonuç ALEYHİMİZE çıktı ve yazıldı.** Tabloda **iki farklı aletin** sayısı yan yana duruyordu (ADR-0057 ihlali). Beş kol da **yayımlanan** boru hattıyla yeniden puanlandı; **kalibrasyon geçti** (kendi `harness_tablo.json`'umuz **alan alan** yeniden üretildi), her kol **iki kez** koştu bayt bayt aynı. **702 atfın tam olarak 1'i değişti** ve o damga **rakip aleyhine yanlış** basılmıştı: `3.1 flash-lite` **`1/152` → `0/153`**. ⇒ **Tek deterministik üstünlüğümüzde artık eşitiz. Karşılığında alınan şey eşit sınavdır.**

**Kusur 23 — ikinci sınama: alet tarafı TEMİZ, hakem tarafı AYRI bir gürültü kaynağı.** Deterministik boru hattı üçüncü kez `sha256` birebir çıktı. Aynı adım **canlı bir "aynı girdi farklı sayı" örneği** buldu (id65) ama kökeni **AYRI**: `groundedness.py` her kalemi **tek başına** çağırır, iki koşu arasında paylaşılan bağlam **yoktur** ⇒ fark **aynı prompt'un iki ayrı API çağrısında** farklı çıktı vermesinden gelir — betiğin kendi docstring'inin baştan yazdığı şey (*"claim sınırı hakem-öznel + temp=0 tam deterministik değil"*). **Bu, kusur 23'ün canlı bir örneği DEĞİLDİR.**
⇒ **Hüküm: KAPATILMADI.** Deterministik taraf **temiz** sayılır ama orijinal olayın **kök nedeni hâlâ bulunamadı**; *kapatmak için kendimizi zorlamadık.* `v2`'nin işi kod-tarafı hata aramak değil, **hakem-katmanı gürültüsünü `--runs N` havuzlamasıyla azaltmaktır.**

**Kusur 32 — onarımın ters yüzü: `git clone`'u düzeltti, konteyneri kırdı.** İndeksin künyesi korpus yolunu **indeks dizinine göreli** tutuyor; repo ağacında doğru çözülür, konteynerde indeks **volume'de** olduğu için `/corpus/`'a çözülür ve korpus orada değildir. Bu, taşınabilirlik onarımının **ters yüzüdür**: mutlak → göreli değişikliği `git clone`'u onardı, ama indeksin **repo ağacının dışına** taşındığı tek düzeni **kırdı**. ***Onarım da bir değişikliktir ve kendi regresyonunu ister.*** Tek tesellisi: **sessiz değildi.**

**Kusur 26 · 27 · 28 · 30 — üç insan gözü kapısının SÜİT YEŞİLKEN yakaladığı dört kusur.** Dördü de sayısal kapıların göremediği yerden geldi ve **ikisi aynı gün bizim yazdığımız koddandı**. Testler kusurun *görülmediği* varsayımla yazılmıştı: tırnaksız alıntı · editable kurulum · repo ağacı. ⇒ ***Sayısal kapı, TESTİN KÖRLÜĞÜNÜ görmez.***
*(⚠️ Kapanış bloğunda önce "273 test yeşilken" yazılmıştı ve **yanlıştı** — dördü aynı ana ait değil: 26·27 → süit 267 · 28 → 273 · 30 → 274.)*

### (C) Devir tablosu — kapanış anının SNAPSHOT'ı, geriye dönük düzeltilmez

Kapanış anında **on bir** kusur açıktı: **5a** → `v2` · **6** → `v2`/`B1` · **10** → insan kararı *(sonra kapandı)* · **11** → kendi turu · **12b** → `v2`/`B11` · **23** → `v2` · **24** → kendi turu *(sonra kapandı)* · **25** → `v2` *(sonra kapandı)* · **27** → `v2` · **29** → G20'nin devamı *(sonra kapandı)* · **31** → kayıt.
**Devredilmemiş açık kusur YOKTUR** ⇒ planın kapanışı **geçerlidir**.

### (D) Ölü bağlantılar — kayıt, onarım değil

`docs/adr/**` ve `docs/record/**` içinde `docs/superpowers/` yollarına **29 bağlantı** vardı: **19'u 2026-09-06'dan beri zaten ölüydü** (ve hiçbir yerde yazılı değildi), silme **9 yenisini** ekledi, **1'i** dizin kuralıdır ve canlı kalır.
⛔ **Bu bağlantılar ONARILMAZ.** *`docs/adr/**` ve `docs/record/**` yeniden yazılmaz — onlar "o gün bu belge şunu diyordu" kaydıdır.* **Ölü bağlantı belgelenmiştir, onarılmayacaktır; sessiz kalmasındansa yazılı olması yeğlenmiştir.**

**Elenen.** (1) Planı `record/`'a taşımak — **insan reddetti**: *"plan formatı ajanları yanıltır"* (kutucuklu bir dosya `record/` altında bile **yürütülebilir** sanılır). (2) Yönlendirme kütüğü bırakmak — **insan sadeliği yeğledi**; *dizinde duran bir mezar taşı "tek canlı plan" kuralının ilk istisnası olurdu.*
**Ne KURULMAZ.** Bu ADR *"plan içeriği yedeklendi"* **demez** — gövde **silindi**; görev metinleri, `verify:` şartları ve ara turların anlatısı **gitti.**

### (EK) Silinen "mevzuat kapsamı ve tazelik" tasarımının taşıyıcı özü

> **Bu bölüm spec'in YERİNE GEÇMEZ ve *"tasarım onaylandı"* DEMEZ** — grill'in **girdisidir**. Dokuz madde **açık** durur.

**E.1 · Amaç.** Korpusu kat kat büyütmek **ve** *tarihi belli, tazelenebilir, geri alınabilir bir anlık görüntüye* çevirmek; ürün bunu yaparken **ağa dokunmadan** çalışır. **Canlılık ürünün dışına alınır.** Takas bilerek **tekrarlanabilirlik lehine**: her soruda canlı API (a) **TR IP** ister, (b) API düşerse **ürünü düşürür**, (c) kullanıcının **sorusunu dışarı sızdırır**, (d) ölçümü **yeniden üretilemez** kılar.

**E.2 · Mimari — üç zaman ölçeği, dört dar birim.** `bedesten API → [1 TAZELİK: haftalar] → data/corpus/ (sürümlenmiş anlık görüntü) → [2 İNDEKSLEME: tazelemeden sonra] → data/index/ → [3 ERİŞİM: her soruda, ağa DOKUNMAZ] → answer()`. **Sınır buradadır: erişim katmanı ağı hiç görmez.**
`hakhukuk/mevzuat/` **bugün YOKTUR**; dört yeni birim öngörülüyor: `kaynak.py` (bedesten sözleşmesi — sayfalama, TR IP tespiti, retry **burada gizli**) · `tazelik.py` (**saf fark aritmetiği, ağa dokunmaz** ⇒ sahte `kaynak` ile ağsız ve **$0** sınanır) · `anlik.py` (sürümleme · künye · `sha256` · **atomik yazma** · geri alma) · `indeksle.py` (artımlı gömme).
Ana arayüz `tazele(mod: Tazelik = Tazelik.SOR) -> Rapor`; `Tazelik` enum'u **`SOR` · `OTOMATIK` · `KAPALI`** — **bool bayrak yok**, *"sor" hâli bir `bool` ile ifade edilemez.*
**Niçin ayrı `kaynak.py` (POSD silme testi):** silinirse bedesten'in tuhaflıkları — `pageSize` max 20, `guncellemeTarihi` **60/60 örnekte boş**, TR IP şartı, gövde sarmalaması — **dört çağırana birden** yayılır.

**E.3 · İki sert kural — yumuşatılmadan.** **1 · TR IP yoksa cihaz içi fallback YOKTUR** — `TrIpGerekli` atılır, `tazele()` onu **yutmaz**; **sessiz düşürme bu repoda hata sınıfının kendisidir.** Kullanıcı **asla tarihsiz kalmaz**. **2 · Kısmi tazeleme diske YAZILMAZ — hepsi ya da hiçbiri.** *14 belgeden 9'u inip ağ koparsa korpus **bayt bayt eski hâlinde** kalır; aksi hâlde künyedeki tarih ile içerik ayrışır ve **o ayrışma hata vermez, yalnız sessizce yanlış olur**.*
**Mahremiyet hükmü:** tazelik çağrısı **soruyu taşımaz** — kullanıcının ne merak ettiğine dair **tek bit** bilgi gitmez.

**E.4 · KAT MERDİVENİ ve kapısı.** Her kattan sonra **aynı 80 soruyla** `recall@10` yeniden ölçülür; **hakem gerekmez, $0**; eşik koşudan **önce** yazılıdır: `recall@10 ≥ 0,9500 → kat GİRER`.

| kat | eklenen tür | yeni belge | ≈ madde |
| :--- | :--- | ---: | ---: |
| 1 | `KHK` + `CB_KARARNAME` | 119 | ~33.000 |
| 2 | `TUZUK` | 110 | ~4.000 |
| 3 | `YONETMELIK` | 172 | ~8.000 |
| 4 | `CB_KARAR` | 4.361 | ~76.317 |
| 5 | `KKY` | 4.043 | ~126.343 |
| | **eklenen toplam** | **8.805** | **247.660** |
| | bugünkü korpus | 892 | **40.496** |
| | **beş kat sonrası** | **9.697** | **288.156** |

⭐ **En değerli bulgu: merdiven baştan DOĞRUYMUŞ; hedefi kaydıran tek bir satırdı.** Spec hedefi **340.303** ilan ediyordu, aradaki **52.147** madde açık kalıyordu. Kaynağı ölçüldü: spec'in `KANUN` satırı **917 × 102,7 = 94.145** sayıyor; **gerçek oran 45,3991 madde/belge** (40.496 ÷ 892, korpus satır satır sayılarak doğrulandı) ⇒ **sapma 2,26×**. Düzeltme: `340.303 − 94.145 + 41.631 = **287.789**` — merdivenin **288.156**'sıyla **%0,13 farkla** örtüşüyor. ⇒ **Hedef ~288 bin madde olarak okunmalıdır** ve **340.303'ün bütün türevleri** (697 MB indeks · 2,1 GB RAM · ~803 ms sorgu · ~1,5 sa gömme) aynı oranda yeniden hesaplanmalıdır.
⚠️ **ÜÇÜNCÜ tutarsızlık:** spec'in `KKY` satırı **kendi içinde çarpmıyor** (`4.043 × 31,2 = 126.141,6` iken tablo **126.343**, −201,4) ⇒ düzeltilmiş hedef iki değer alabiliyor (**287.789** ↔ **287.589**); fark **200** ve **tamamı bu satırdan**.
⚠️ **Diğer türlerin oranları DOĞRULANAMAZ — ve şüphelidir.** Bütün oranlar **tür başına 6 belgelik** bir örneklemden; tabakalanma **yazılı değil**. Doğrulayabildiğimiz **tek** türde yöntem **2,26× şişti**; özellikle `CB_KARARNAME`'in **564,0** değeri (KANUN'un 5,5 katı) **tek bir doğrulaması olmadan** duruyor. **Bu tuzak 1.11'in tam sınıfıdır.**
**Kapının kendi sınırı — spec'in en dürüst cümlesi, aynen korunur:** kapı yalnız ***"eskiyi bozmadı"*** der; ***"yeniyi buluyor" DEMEZ*** — 80 sorunun hepsi kanun düzeyindedir, yönetmelik düzeyinde eval sorumuz **yoktur**.

**E.5 · GRİLL'E GİRECEK DOKUZ MADDE** *(her biri **açık**, hiçbiri burada kapatılmıyor)*

| # | madde | niçin |
| :-- | :--- | :--- |
| **1** | **Hedef sayı düzeltilsin** — beş kat **288.156**, 340.303 değil | fark **52.147**, kaynağı `KANUN` satırının 2,26× şişkin oranı; türevleri yeniden hesaplanır |
| **2** | **n=6 örneklemi damgalansın ya da yenilensin** | ölçülebilen tek satırda **2,26×** sapma; tabakalanma yazılı değil. Tuzak 1.11'in reçetesi: tabakalanmış örneklem ya da **%50 emniyet payı**. `CB_KARARNAME 564,0` tek başına sorgulansın |
| **3** | **Eşit sınav faturası spec'e YAZILSIN** | indeksi sonradan değiştirmek eşit sınav kanıtını **bozar** ve kolların yeniden ölçülmesini gerektirir (~$1,35); spec bunu **ne anıyor, ne bütçeliyor** |
| **4** | **Mülga kapısı SAHTE — gerçek kapı yapılsın** | plan yeni satırlara `"mulga": False` **sabitini** basıyor; bedesten mülga bilgisi vermiyor ⇒ *"hiçbir kaynak mülga olmamalı"* testi **her zaman geçer, boş bir tekrardır**. Bu, 2026-09-07'de kapatılan kusurun **ölçekte ve sessizce** geri gelmesidir |
| **5** | **Künye sözleşmesi bugünkü biçime çekilsin — plan kusur 32'yi YENİDEN ÜRETİYOR** | plan `mtime`'ı geri getiriyor ⇒ sonraki `Retriever.yukle` **`SystemExit`** ile düşer. Ayrıca **ikinci bir künye dosyası** kuruyor ⇒ ayrışırsa **vatandaşın gördüğü tarih sessizce donar** |
| **6** | **Kapının ÇÖZÜNÜRLÜĞÜ yazılsın** | `n=80` ⇒ adım **0,0125**; `≥ 0,9500` **gürültü payı SIFIR** demektir (tek soru düşerse kat **reddedilir**). `≥ 0,9500` mü, `≥ 76/80` mi, `≥ 75/80` mi — **üçü farklı kapıdır** ve koşudan **önce** yazılmalı |
| **7** | **Atıf doğrulayıcı ölçeğe karşı yeniden ölçülsün + `kanun_no = UUID` kararı geri alınsın** | *korunma aletten değil ÖRNEKLEMDEN geliyordu*; 892 → ~9.700 belgede çakışma yüzeyi büyür ve `AYRISTIRILAMADI` artışı **`DOGRULANDI` paydasını küçültür**. Ayrıca plan bedesten **UUID**'sini kanun-numarası alanına yazıyor ⇒ **ekranda UUID** |
| **8** | **Dokuz bayat satır damgalansın** | *"korpusun güncellik tarihi YOK"* · *"`mulga` retriever'da kullanılmıyor"* · *"800 kaynağın 2'si mülga"* (**0**'landı) · *"büyüme ~1,5×"* · *"340k'da ~51 ms"* (**16× iyimser**, ~0,8 sn) · *"indeks ~120 MB"* · **79,1 MB ↔ 83 MB** aynı dosya için |
| **9** | **İki isim/arayüz kararı yazılsın** | (a) plan **kendi `Durum` enum'unu** koyuyor — ürünün altı üyeli `Durum`'uyla **aynı adda, aynı paket ağacında, farklı kapalı küme** ⇒ öneri **`TazelikDurumu`**; (b) `_gomulecek_metin` **kopyalanıyor** — bu bir **karar** olarak yazılsın, docstring uyarısı olarak değil |

**Bağımlılık sırası:** `G1 (kapsam kapısının aleti + ÇIPASI) → {G2 kaynak, G3 kimlik, G4 tipler+tazelik} → G5 anlik → G6 indeksle + ÖLÇEK KAPISI → G7 kat kat (>$1 ⇒ DUR-SOR) → G8 tazele()+CLI → G9 ADR+kayıt`.
**G1'in birinci olmasının tek sebebi tuzak 2.17'dir:** spec'in kapısı `recall@10 ≥ 0,9500` diyor ama *"bu sayı hangi dosyada, hangi aletle, hangi birimde ölçüldü"* **yazılı değil.** *(Not: indeks künyesinde `rrf_k` bugün hâlâ **60** yazıyor — künye kodla uyuşmuyor.)*
**G5/G6 bir kapı değil, gözlem taşır:** `kayitTarihi`'nin gerçekten bir **değişim sinyali** olduğu **kanıtlanmadı**; çıkmazsa tasarım değişir ve **her belgeyi indirmek** gerekir.

**E.6 · BÜTÇE GERÇEĞİ — mesele para değil, SIRA KARARI.** Modal **$29,19** · OpenRouter **$2,041996** · κ borcu **$2,81** ⇒ **açık −$0,77**.
**Yazılı bedel koşulabilir** (G1-G6, G8, G9 **$0**; kapsam kapısının her kattaki ölçümü **$0** — spec'in en iyi tasarım kararı). **Yazılmayan bedel koşulamaz:** indeks değişince eşit sınav **yeniden kurulmak zorundadır** (**~$1,35 … $2,5+**, havuzda **beş** kol var ve tek başına Sonnet-5 kolu **$1,1932** tuttu — tahmin $0,82 idi, tuzak 1.11).
> **Bu bir *"önce para"* meselesi değil, *"önce SIRA"* meselesidir ve insan kararı ister:** **(a)** κ borcu ödenip `v1.0` kapatılana kadar **beklesin**; **(b)** iş önce koşsun ama *"yayımlanan kütle ve eşit sınav kanıtı, korpus değiştiği anda **ASKIYA ALINIR**"* cümlesi **kabul edilmiş bedel** olarak yazılsın. **Bugün tasarım ikisini de söylemiyor.**

**E.7 · Ne KURULMAZ.** Bu bölüm spec'in **yerine geçmez** · *"tasarım onaylandı"* **demez** · bir **iş emri değildir** · **bedesten sayıları doğrulanmadı** (917 KANUN · 4.361 CB_KARAR · 4.043 KKY · `pageSize` max 20) — bu tur **$0/ağsız** koştuğu için yeniden ölçülmedi, **çürütülmedi de** · **Modal bakiyesi bugün doğrulanmadı.**

### EK-2 — model kartından çıkarılan ÖLÇÜM AÇIK SORULARI (2026-09-13)

⚠️ **Wilson aralıkları kartın içinde hesaplanmıştı** (`z=1,96`, `n=80`) ve **başka hiçbir dosyada yoktu.**

| oran | nokta tahmin | Wilson %95 | genişlik |
| :--- | ---: | :--- | ---: |
| **isabetsizlik 8/80** | 0,1000 | **[0,0515 – 0,1851]** ≈ [4/80 – 15/80] | **13,4 puan** |
| aşırı-red 4/80 | 0,0500 | [0,0196 – 0,1216] ≈ [2/80 – 10/80] | 10,2 puan |

⇒ ***"8/80 ↔ 6/80" gibi farklar bu genişliğin ÇOK ALTINDADIR.*** İkinci örnek: M2'nin kuantumu `1/66 = 1,52 puan`, yani `+0,000` ile `±1 kalem` **ayırt edilemiyor**.
**2 · DEV ile TEST aynı sınav değil** — ayrım **madde uzunluğuna göre katmanlanmamış**; bileşim farkın **%81**'ini açıklıyor. Yeniden katmanlama *"donmuş TEST'i açar"* diye **reddedilmişti**.
**3 · Her sayı tek hakem ailesinin hükmü.** *Soru: κ hangi eşiğin altında kalırsa kartın sayıları yeniden koşulur?*
**4 · Manşet ölçüt bir ÇARPIM:** `kütle = coverage × A1` ve iki çarpan **farklı örnekleme birimlerinden** gelir (`coverage` **kalem** başına, `A1` **iddia** başına ve yalnız cevaplananlar üzerinden makro) ⇒ çarpıma ikili bir güven aralığı **doğrudan uygulanamaz.** *Aynı sebep `v1.0` kapısının δ'sının ölçümden değil **insan kararından** gelmesinin gerekçesidir.*
**5 · Dış karşılaştırılabilirlik yok.** Sayılar kendi CANON setimizde üretiliyor ve **kimse bu sette koşamaz**; bedeli **doğrulanabilirliktir**. Bir seçenek — karar değil — CANON'un kamuya açık bir alt kümesini yayımlamak; bedeli de ölçülü (**donmuş TEST bir kabul testidir, yayımlanması onu yakar**; DEV ise **seçim yapılmış** settir).
**6 · Yer-gerçeği denetimi.** Üç kolda da **sınır durum** sayıldı ve hiçbirinde sayıya katılmadı ⇒ **üç isabetsizlik sayısı da ALT SINIRDIR.** Etiketler **değiştirilmedi**: *sonucu gördükten sonra yer-gerçeğini oynatmak yanlılık korumasının doğrudan ihlalidir.*
**7 · Ölçüm aleti hâlâ onarım altında.** Faz 0'da beş alet kusuru bulundu ve **hiçbiri sayısal kapıya takılmadı**; dedektör **üç kez** yanıldı ve `exact_reject`'in **kör mod dalı açık borç.** *Soru: "gözle okuma" kalıcı bir kapı mı, yoksa aletin olgunlaşmasıyla azalması beklenen bir maliyet mi?*

---

<a id="adr-0084"></a>
## ADR-0084 — κ borcu ADR-0074'ün koşuluyla kapandı; kapının üç maddesi yeni birimde okundu: GEÇTİ

**🟢 Yürürlükte · 2026-09-13** (ölçüm 2026-09-12) · Karar: **mekanik** (ön-kayıtlı formül)

**Ön-kayıt (aynen alıntı, sayı görülmeden yazıldı).** ADR-0074'ün kapanma koşulu tek cümleydi: *"Borcun kapanma koşulu tektir: `3.5 Flash` kolunun **aynı ikinci hakemle** puanlanması."*
**Ölçülen.** **Koşul karşılandı** — rakip kolu (`3.5 Flash`, 80 cevap) **aynı ikinci hakemle** (`claude-sonnet-5`) yeniden puanlandı; üretim yeniden koşulmadı, **yalnız hakem değişkeni oynadı.**

| madde | ölçü | çıpa (GÖZ-katı) | eşik (−2,0 p) | BİZ | marj çıpaya göre | hüküm |
| :--- | :--- | ---: | ---: | ---: | ---: | :--- |
| **(1)** kütle | `gpt-4o-mini` | 0,7425 | 0,7225 | **0,8011** | **+5,86 p** | ✅ GEÇTİ |
| **(1)** kütle | `claude-sonnet-5` | 0,6183 | 0,5983 | **0,6940** | **+7,57 p** | ✅ GEÇTİ |
| **(2)** isabetsizlik | *(hakemden bağımsız)* | 8/80 | — | **8/80** | tanım gereği | ✅ |
| **(3)** M5 | *(hakemden bağımsız)* | base 0,4697 / 0,4739 | — | 0,3899 / 0,4057 | **−7,98 / −6,82 p** | ✅ GEÇTİ |

Çözünürlük bandı `n=80` için **1,25 puan/kalem**; marjların hepsi bu bandın **kat kat** üstünde ⇒ hüküm okuma seçimine bağlı değil, **iki bağımsız hakem ailesi aynı yönde.**
**Madde (2) ve (3) neden hakemden bağımsız taşındı — *"taşındı" ≠ "bakılmadı"*.** Bu turda ağırlıklar **hiç değişmedi** ve bu sayılar **hakem-öncesi** deterministik/gözle-okuma katmanından geliyor; ikinci hakemin ölçtüğü eksen yalnız `faithfulness`'tır.

**Karar (a) — kapı üç maddesiyle GEÇTİ, yeni birimde.** İkinci hakemde marj **daha geniş** çıktı. `v1.0`'ın önündeki madde (b) — *"her sayı hâlâ tek hakem ailesinin hükmü"* — **kapandı**.
**Karar (b) — κ DEĞİŞMEDİ; kapanan şey κ değil, EŞİT SINAVIN YOKLUĞUYDU.** κ **0,534 / 0,409** bu ADR'de **hiç yeniden ölçülmedi** ve **aynı kalıyor**. ***"κ düzeldi" cümlesi KURULMAZ.***
**Karar (c) — ADR-0074(b) ↔ ADR-0077 gerilimi uzlaştırılır, iki yerde damgalanır.** *Kırılgan bir sayı, ikinci bağımsız bir hakemle aynı yönde doğrulanınca artık yalnızca "tek ailenin hükmü" değildir — kırılganlığın kaynağı (κ zayıf) durmaya devam eder, ama sürüm eşiğini bloke eden özel biçimi ortadan kalkar.* ADR-0077'nin metni **değiştirilmedi**, yalnız işaretçi eklendi.
**Karar (d) — ADR-0032'nin üç aile kuralı hâlâ 2/3'te; açık gerilim, eksiklik olarak yazılır.**

**Ne KURULMAZ.** ① *"İki hakem geçti ⇒ sayı kesin"* — κ zayıf kalmaya devam ediyor, üçüncü aile hiç ölçülmedi · ② *"κ düzeldi"* · ③ **ürün-yolunun kütlesi (0,7792) kapı maddesine girmez** (eşit sınav değil) · ④ kuantizasyon eğrisi bir **ifşadır**, sürüm önerisi değil · ⑤ **öz-tercih sonucu hiçbir hükme girmez** — *hakemin kendi ailesini bizden daha sert cezalandırması madde-1 hükmünü değiştirmiyor, yalnız "hakem bizi kayırdı" itirazını bağımsız olarak zayıflatıyor* · ⑥ **bu ADR `v1.0` ETİKETİ VERMEZ** · ⑦ *"rakip kolu her iki hakemle de aynı sayıyı verdi"* denemez — `3.5 Flash`'ın kendi kütlesi **düştü** (0,7425 → 0,6183); **değişmeyen şey kapının hükmüdür**, rakibin mutlak sayısı değil.
**Reddedilenler.** κ'yı yükseltmek için ek koşu · üçüncü aileyi bu turda eklemek · **ADR-0077'yi geriye dönük düzenlemek** (*audit trail: eski karar olduğu gibi durur, işaretçi **eklenir**, üstüne yazılmaz*) · bu ADR'de `v1.0` ilan etmek.

---

<a id="adr-0085"></a>
## ADR-0085 — Tek model kartı, ürün tonu, ve iki yeniden adlandırma

**🟢 Yürürlükte · 2026-09-13** · Karar: insan · Tuzak **2.21** · `9e3d9bc` · `6483c36` · `61bae1b`

**Bağlam — iki kart iki kez sessizce ayrıştı.** Depo iki model kartı taşıyordu ve ikisi **elle** güncelleniyordu:

| bayat yer | ne diyordu | gerçek |
| :--- | :--- | :--- |
| `HF_KARTI` §2 | *"soruların ~%5'inde cevap boş dönmektedir"* | [ADR-0080](#adr-0080) bunu **2026-09-11'de kapattı** (4/80 → 0/80) |
| `HF_KARTI` §3 | *"toplam etkisi **ölçülmemiştir**"* | **2026-09-11'de ölçüldü**: 65/80 bayt değişti, kütle 0,8011 → 0,7932 |
| `MODEL_CARD` §7.9 ↔ §8 | §7.9 *"düzeltilmedi"*, §8 aynı kusurun **0/80** ölçüldüğünü | dosya **kendi içinde çelişiyordu** |
| `MODEL_CARD` (5 yer) | *"arama indeksi yayımlanmadı"* | **public dataset**; `indir.py` onu zaten **varsayılan** kullanıyor |

**Birinci ve ikinci satır HF'te yayındaydı:** depo, **düzeltilmiş bir kusuru herkese açık biçimde *"açık"* diye ilan etmeye devam ediyordu.**

**Karar 1 — TEK KART, ve HF sürümü ÜRETİLİR.** `MODEL_CARD.md` **tek kaynaktır**; `docs/HF_KARTI.md` **silindi** (seçim gerekçesi ölçüldü: konvansiyonel ad · GitHub kökünde görünür · **82 atıf** · test listesinde; `HF_KARTI`'ya **canlı atıf yoktu**). **Ayrışma yapısal olarak imkânsızlaştırıldı:** `scripts/hf_kart.py` HF'in `README.md`'sini karttan **üretir** (YAML ön bilgi + göreli bağlantıları mutlaklaştırma — *HF deposunda `docs/` yoktur, göreli bağlantı orada **sessizce ölür***), dört testle korunur.
**Karar 2 — Kart ÜRÜN BELGESİDİR, onarım günlüğü değil.** Üstü çizili düzeltmeler ve damgalar karttan **çıkarıldı**; tarihçe `docs/adr/` ve `docs/record/`'da **tam olarak durur.** ⚠️ *Bu, "çelişkiyi iki yerde damgala" kuralına **aykırı değildir**: kural **araştırma kaydını** bağlar. **Kartın iki yerde sessizce bayatlaması zaten bu yığılmanın sonucuydu** — 1.200 satırlık üstü çizili tarihçe içinde iki yanlış cümle kimsenin gözüne çarpmadı.* Karttan çıkarılan her bölümün yeni evi **yazılı olarak doğrulandı**; yalnız biri hiçbir yerde yoktu ve **taşındı** (Wilson aralıkları → [ADR-0083](#adr-0083) EK-2).
**Karar 3 — İKİ YENİDEN ADLANDIRMA.** GitHub `Rfetha/Hukuk-SLM` → **`Rfetha/HakHukuk`** · HF `…-4B-v0.3-Q4_K_M` → **`Rfetha/HakHukuk-4B-GGUF`** · GGUF **dosya adı değişmedi**. *ADR-0071'in kalıbı **dosya** için geçerlidir, depo için değil; depo adının tek bir kuanta kilitlenmesi S17'de ölçülen varyantlara yer bırakmıyordu.* **Ürün adı `HakHukuk` DEĞİŞMEDİ** — ad kod düzeyinde gömülüdür.
**Doğrulandı, varsayılmadı:** eski ad **yönlendiriyor** · pinli revizyon **yeni ad altında çözülüyor** ⇒ kimlik kapısı **etkilenmedi** · `private = False` · kart **token'sız** bir süreçle geri okundu.
⛔ **Bu karar GRILL KARARI 6'YI TERSİNE ÇEVİRİR** (*"HF deposunun ADI aynı kalır"*). **Gerekçesi korundu** — değişen **depo** adıdır, **dosya** adı değil; *grill kararı ikisini ayırmamıştı, bu ADR ayırır.*

**Karar 4 — Aşırı-red ekseni TEK BİRİME çivilendi.** Eksen depoda **dört ayrı belgede dört ayrı sayıyla** yazılıydı. **Kök sebep: koşullu ölçütün koşulu adında yoktu** — *"temiz çekinme"* adı üç Gemini kolunda yalnız **altın-gelmiş** kalemleri sayıyordu, bizim ve Sonnet-5 kolunda **tüm** çekinmeleri.
**Bağlayıcı birim: altın geldi ve model yine de sustu.** *Altın hiç getirilmemişken susmak **doğru davranıştır** ve bu eksende sayılmaz.*

| kol | aşırı çekinme (gözle, tek birim) |
| :--- | ---: |
| HakHukuk | 4/80 |
| `gemini-3.1-flash-lite` | 5/80 |
| `gemini-3.5-flash-lite` | 5/80 |
| `gemini-3.5-flash` | 7/80 |
| **`claude-sonnet-5`** | **3/80** |

⇒ **Yayımlanan bir sayı değişti ve aleyhimize:** kart *"aşırı çekinmede iki model eşittir"* diyordu; **Sonnet-5 öndedir.** Düzeltmeyi mümkün kılan şerh kalibrasyon dosyasının gerekçe metninde **zaten yazılıydı** ama **satıra hiç yansımamıştı.**
**Karar 5 — README'ler KISALTILDI ve karta işaret eder hâle geldi.** 380/372 satır → **~125 satır**. *README'nin işi kartı tekrarlamak değil, ona götürmektir.*
**Ne KURULMAZ.** *"Kart artık bayatlamaz"* **DEMEZ** — üretim betiği yalnız **HF ile depo arasındaki** ayrışmayı kapatır; bu turda üç bayat iddia bulundu ve üçü de **elle** yakalandı, **otomatik bir kapı YOKTUR.**

---

<a id="adr-0086"></a>
## ADR-0086 — Kayıt katmanı kompaktlandı: 103 dosya → 12

**🟢 Yürürlükte · 2026-09-13** · Karar: insan
**Tasarım:** [`specs/2026-09-13-kayit-katmani-kompaktlama-design.md`](../superpowers/specs/2026-09-13-kayit-katmani-kompaktlama-design.md)
**Geri dönüş noktası:** `301a82f`

**Bağlam — ölçüldü, tahmin edilmedi.** `docs/adr/**` + `docs/record/**` = **103 dosya · 1,40 MB · ~466 bin token.** Şişkinlik iki sınıfta: **(1) dizin/kayıt tabloları** (en sık okunanlar) — `research_log/README.md` **672 B/satır** · `yurutme-tuzaklari.md` **286** · `adr/README.md` **273**, toplam **156 KB**; `research_log/README.md`'nin `Kanca` sütunu her girdinin içeriğini tekrar taşıyor, **index değil ikinci kopya**. **(2) anlatı gövdeleri** — normal yoğunlukta (60 B/satır) ama uzun; kod fence'i toplamın yalnız **%6'sı** ⇒ kısaltma **düzyazıdan** çıktı.

**Çarpışan kural — bilinçli sapma.** ADR-0024'ün iki pazarlıksız kuralından biri: *"`research_log/` ve `adr/` yerinde kalır — makaleyi haftalar sonra yeniden kurabilme garantisi kronolojik kayıt taşınırsa kırılır."* **Bu tur o kuraldan bilinçli olarak sapar (insan kararı)** — tıpkı 2026-07-24'te 26 ADR → 1 dosya ve 38 log girdisi → 1 dosya birleştirmesinde sapıldığı gibi. Kuralın özü (**içerik silinmez, sayılar kaybolmaz**) damıtma kuralıyla korunur; silinenler git geçmişinde `301a82f` ve öncesinde durur.

**Karar.** `docs/adr/` → **5 dosya** (`README` + üç `kararlar-*` + `gemma4-12b-dersler`), `docs/record/` → **7 dosya** (`README` · `kollar` · `yurutme-tuzaklari` · `gemma4-12b-kronoloji` · üç `kronoloji-*`).
**Numara/tarih aralığıyla gruplama seçildi, tema ile gruplama ELENDİ:** *"ADR-0057 hangi dosyada"* sorusu **aritmetikle** cevaplanır; tema okumada iyi, aramada kötü. Her ADR ve her log girdisi bir **çapa** taşır ⇒ repo genelindeki **2117** `ADR-00NN` metin göndermesi yaşar.
⛔ **`gemma4-12b-dersler.md` Bölüm B silinemez:** emekli hattın `ADR-0001..0026` numaralarına repoda **319** gönderme var ve **10'u CLAUDE.md'de yürürlükte** anılıyor (`0003 · 0010 · 0011 · 0016 · 0017 · 0018 · 0019 · 0023 · 0025 · 0026`).

**Damıtma kuralı — pazarlıksız beş madde.** ① **Her sayı birebir korunur** (metrik · n · hakem · seed · kaynak dosya yolu) · ② her **Ders** bloğu korunur · ③ **her negatif/çürütülmüş bulgu korunur** — *bu hattın en değerli bulguları kendi planlarının çürütülmesidir* · ④ her **elenen alternatif + gerekçesi** korunur · ⑤ kesilen: oturum-oturum anlatı · tekrar eden bağlam kutuları · komut dökümleri · aynı sayının üçüncü tekrarı · ölü işaretçiler.

**Silinenler.** `record/2026-09-10-hp-hat-a-hat-b-kapanan-gorevler.md` (67 KB — **kendi başlık kutusu** *"bu bulguların sayıları `docs/record/` ve `docs/adr/` içinde zaten vardı"* diyordu) · `record/sprint2/defter.md` (69 KB — icra dokümanı `_arsiv/sprint2.md` **zaten silinmişti**, yarım kayıt; kaynak girdiler #42-#47 duruyor) · `record/sprint1/` (3 dosya, 38 KB — sonuç tablosu zaten *"parite iddiası değil"* damgalıydı).
**KORUNAN:** `record/gemma4-12b-kronoloji.md` — *tek dosya kazandırır ama 38 girdinin tüm sayılarını götürür; risk/kazanç kötü.*

**Ne KURULMAZ.** ① *"Hiçbir şey kaybolmadı"* — **kaybolan var**: silinen üç kaydın anlatısı ve damıtmada kesilen oturum ayrıntıları yalnız git geçmişindedir · ② *"ADR-0024 hâlâ yürürlükte"* — **kısmen değil**; `record/` ve `adr/` bu turda **yeniden yazıldı** ve bu, kuralın ikinci bilinçli ihlalidir · ③ `outputs/eval/**`, `scripts/**`, `knowledge/**` içindeki tarihsel yol göndermeleri **onarıldı, silinmedi.**

**Kapanış — ölçülen sonuç ve iki tutturulamayan kapı.** *(Turun planı ve tasarımı 2026-09-13'te
**silindi** — emsal ADR-0083 §D, kapanan plan kayda dönüşür. Yalnız o iki dosyada yazılı olan
dört bulgu buraya taşındı; yukarıdaki `**Tasarım:**` işaretçisi bilerek onarılmadı, "o gün bu
belge oradaydı"yı kaydeder. Geri alma: `git show 301a82f:<yol>`.)*

Sonuç: **103 dosya · 1,40 MB · ~466 bin token → 12 dosya · 608 KB · ~208 bin token** (dosya
%88, hacim %55 azaldı). `pytest tests/` **335 geçti, 2 xfail**; ölü bağlantı **0**, çözülen
çapa **297**.

1. **`yurutme-tuzaklari.md` için `≤ 25 KB` kapısı TUTTURULAMADI — 44 KB'de kaldı.**
   Ölçüm: `52.472 → 43.779 bayt` (`git show 301a82f:docs/record/yurutme-tuzaklari.md | wc -c`
   ↔ `wc -c`). Sebep ölçüldü, tahmin edilmedi: bu dosyada **kesilecek anlatı yok** — 85 satırın
   her biri ayrı bir derstir ve `ne olur` sütunu dersin kendisidir. Damıtma kuralı ①-② (*her sayı,
   her ders korunur*) bayt hedefinin **önünde** tutuldu; 202 benzersiz sayının tamamı denetlendi
   ve **hiçbiri düşmedi**. ⚠️ Tasarımın kendi tablosu başlangıcı **48 KB** diyordu, ölçülen
   **52 KB**'dir — hedef daha baştan yanlış tabandan konmuştu.
2. **Token hedefi aşıldı: ~208 bin.** Tasarım *"~466 bin → ~190 bin"* yazdı, kapanış bloğu hedefi
   *"~200 bin"* diye andı ⇒ aşım **%4** (200 bine göre) / **%9,5** (190 bine göre). Aynı sebep:
   kalan hacmin tamamı sayı ve ders. **Hedef iki farklı yerde iki farklı sayıydı; ikisi de
   tutmadı ve ikisi de burada durur.**
3. **`gemma4-12b-kronoloji.md` SİLİNMEDİ** — bir dosya ve 38 KB kazandırırdı, karşılığında emekli
   hattın **38 girdisinin numarası** ölürdü. Risk/fayda tutmuyor. *(Yukarıdaki* **KORUNAN** *satırı
   aynı kararı taşır; burada, tasarımın çöp listesinde `KALSIN` damgasıyla duran maddenin karşılığı
   olduğu için tekrarlanır.)*
4. **Bir hata yapıldı ve geri alındı: toptan yol değiştirme bir ALINTIYI yeniden yazdı.** `sed`
   ADR-0024'ün **kendi kural cümlesindeki** `research_log/` dizesini de değiştirdi — yani tarihsel
   bir alıntının metnini bozdu. Fark edildi, geri alındı. **Ders: alıntı, yol onarımının kapsamı
   değildir**; toplu değiştirmede değişen her satırın alıntı olup olmadığı ayrıca görülür.
