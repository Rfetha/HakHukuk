# Yürütme tuzakları — yeni hat (Qwen3.5-4B)

> **Bu belge ne:** *"hata vermeden yanlış sonuç üreten"* kalıpların tek listesi. **85 satırın hepsi bu
> hatta fiilen ısırdı**; hiçbiri teorik değil. Bir koşuya başlamadan önce ilgili bölümü oku.
>
> **12B karşılığı:** [`../adr/gemma4-12b-dersler.md`](../adr/gemma4-12b-dersler.md) Bölüm A —
> orada **base-agnostik dersler**, burada **bu hattın yürütme tuzakları**.
> **Kaynak:** `sprint1.md` *(silindi)* uyarı blokları + kronoloji [#39](kronoloji-39-52.md#39) ·
> [#40](kronoloji-39-52.md#40) · [#41](kronoloji-39-52.md#41).
> **Ekleme kuralı:** yeni bir sessiz-bozulma çıkarsa **aynı gün** buraya bir satır + kronoloji girdisi.
> Kalıcı karar doğuruyorsa ayrıca ADR.
>
> ⚠️ **Numara çakışmaları — bilinçli olarak DÜZELTİLMEDİ** (numaralar depo genelinde atıf alıyor):
> **2.17 iki ayrı tuzaktır** (önbellek anahtarı ‖ çıpasız kapı maddesi) · **5.8 iki ayrı tuzaktır**
> (modül taşıma ‖ WSL2 belleği) · **5.9 iki ayrı tuzaktır** (`git stash` ‖ `pgrep` kendini yakalar).
> Atıf verirken numara **tek başına yetmez**, tuzağın adı da yazılır.

**Neden bu belge var.** Bu hattın hata sınıfı **çökme değil, sessiz yanlışlık**. Sunucu açılır, istek
**200** döner, JSON gelir, skorlama sayı üretir, tablo dolar — ve sayı **yanlış bir şeye aittir**.
Regex omurgası boş çıktı üzerinde de çalışır, `--data` varsayılanı **doğru boyutta** yanlış set verir,
kuantizasyon yarıda kesilse de dosya **diskte durur**.

> **Tek kural:** bir sayı üretildiğinde *"bu sayı neyin sayısı"* sorusunun cevabı **künyeden**
> okunabilmeli. Okunamıyorsa **sayı yoktur.**

---

## 1. Üretim / eval

| # | tuzak | ne olur | korunma | kaynak |
| :-- | :--- | :--- | :--- | :-- |
| 1.1 | **`--data` düşürülür** | Varsayılan `data/train/grounded_qa/test.jsonl` — CANON değil, **eğitim setinin komşusu**; `--n` havuz boyutuyla aynı olduğu için çıktı *doğru görünür* | `--data` her modda **açıkça** yaz | sprint1 CP2 |
| 1.2 | **`--thinking off` düşürülür** | `</think>` kapanmaz → `content=''`, HTTP **200**, sıfır hata | künyede `thinking=off` gör; erken patlama kapısı var ama bayrak yine zorunlu (ADR-0030) | #39 B1 |
| 1.3 | **Şablon dalı yanlış render** | minja bir dalı yanlış değerlendirdi (12B); model **hiç durmadı**, girdiyi tekrarladı, hiçbir aşamada hata yok | `/apply-template` çıktısını **gözle** oku; `enable_thinking` true/false **farklı** render vermeli | #38 · #39 kapı 2 |
| 1.4 | **`--max-chunk-chars 900` düşürülür** | Eval-mirror kırılır — model eğitildiğinden **uzun bağlamla** ölçülür (ADR-0011 değişmezi) | CP2/CP6 bayrakları birebir | sprint1 CP6 |
| 1.5 | **`--n` / seed kayması** | Kıyasın iki tarafı farklı örneklem → delta anlamsız | n = 80/80/70/80/80/80, seed **3407** | sprint1 CP2 |
| 1.6 | **Güç durumu** | Pilde **7,9** t/s, şarjda **134** t/s — **17×**. Performans sayısı prize bağlı | şarj durumunu künyeye yaz | #39 B2 |
| 1.7 | **Yanlış runtime ile kıyas** | Çıpalar Q4_K_M GGUF + llama-server ile üretildiyse `--adapter` (transformers, 4-bit) yolu **kıyaslanamaz** sayı verir — tablo yine dolar | özne hep aynı taşıyıcıdan; adaptör **merge edilip GGUF'a** gider | #41 §2 |
| 1.8 | **`--thinking on` + bütçe = "artırırsan düzelir" sanısı** | Base M1/M2/M5'te `</think>`'i **hiç** kapatmıyor: kesilme değil **sonlanmama**. 4096 → 32768 (**8×**) hiçbir şey değiştirmedi, Q8_0'da da aynı; `temp 0.6` yarısını kurtardı. Bütçe artırmak **saatleri yakar ve yine "geçersiz koşu" yazar** | düşünen model + belirsiz istem = **`--think-budget N` zorunlu**; bütçe **ön-kayıtlı**, künyeye ve maliyet muhasebesine yazılır | #42 |
| 1.9 | **900-kar klip numaralı listeyi ortadan keser** | Kaynak `"…madde 309, 310, 311,"` diye biterse model *"birebir alıntıla"* talimatıyla **saymayı sürdürüyor** (`…1682, 1683, 1684`); 8 örnekte 1'i. Hata yok, cevap dolu, **çöp**. Yalnız düşünce açıkken (thinking-off Sprint 1'de **0/80**) | `finish_reason='length'` sayacı yakalar — **kesik oranı okunmadan sonuç okunmaz**; satır-bazlı döngü metriği bunu **kaçırır** | #42 |
| 1.10 | **n=3 smoke, n=470'i temsil eder sanılır** | CP0 n=36'da *"base M4/M3'te kendi kapatıyor, `τ_g` **35/36** duruyor"* dedi; n=470'te M4 **76/80 zorla**, M3 **60/80**, `τ_g`'nin kazancı **yalnız kendi istem ailesinde**. Smoke hata vermez, **yanlış genelleme** kurdurur | smoke = *"boru hattı çalışıyor mu"*, **asla** *"davranış şu"* | #43 B2·B4 |
| 1.11 | **Duman koşusu parasal kapıyı çarpımla kapatır** | n=5'ten **×16 doğrusal ekstrapolasyon** $0,82 dedi, gerçek **$1,1932** — ön-kayıtlı **$1** kapısı **%45** aşıldı (gerçekleşen **706,6 completion token/cevap × $10,00/M çıktı**). Tahmin üretilir, kapı geçilmiş görünür, para kapının üstünde harcanır. ⚠️ **Dört yüzü var** (#70): doğrusal ekstrapolasyon %45 eksik · düz ön-tahmin %60 fazla · **başka öznenin faturasından** tahmin %41 fazla (hakem maliyeti tamamlama uzunluğuyla değil **iddia sayısıyla** ölçekleniyor) · **farklı moddan** tabakalama (kör modda uzunluklar sıkışır, tabakalama anlamsızlaşır) | duman koşusu **tabakalanmış** seçilir ya da kapıya **%50 emniyet payı** konur; örneklemin tabakalanmış olup olmadığı kapı maddesine yazılır. **Tahmin, puanlanacak öznenin KENDİ profilinden ve AYNI moddan türetilmediyse geçersizdir** | **#65** · **#70** |
| **1.12** | ⭐ **Künye bayrağı SABİT DİZE olarak yazıyor** | 1.1'in künye içindeki hâli ve en sinsisi: `cp0_thinking_gen.sh:89` künyeye `KV q8_0/q8_0` diye **sabit metin** basıyor; satırdaki `$CTX`·`$NGL`·`$PORT`·`$EXTRA_ARGS` değişken. Betik `SERVER_URL` ile **dış sunucuya** bağlandığında o sunucunun KV ayarını **hiç okumaz**. Isırdı 2026-09-11: koşu **kasten fp16**'ydı, künye `q8_0` dedi — **kıyasın ölçmek için var olduğu tek değişken künyede yanlış görünüyordu**. Yakalayan şey künye değil `/proc/<pid>/cmdline` okumasıydı; ironi satırın iki satır üstünde yazılı (*"künyede görünmeyen bayrak sessizce düşer"*) | Künye bayrağı **koşan süreçten** okur ya da **"KV: BİLİNMİYOR (dış sunucu)"** yazar. **Künyedeki her alan ya bir DEĞİŞKENDEN ya bir ÖLÇÜMDEN gelir; elle yazılmış bir alan künyeyi kanıt olmaktan çıkarır** | **#66** |
| **1.13** | **Doğrulayıcı kanun adını GEVŞEK eşleştirip YANLIŞ kanuna çözüyor** | *"Gelir Vergisi Kanunu Madde 73"* → `1319` **EMLAK VERGİSİ** → `MADDE_YOK` = **uydurma** damgası; oysa madde korpusta **var** (`193/Madde 73`, adı parantezli — korpusta **16 kanun** parantezli ad taşıyor). ⚠️ **Asıl tehlike ters yönde ve aynı gün ölçüldü:** yanlış kanunda aynı numaralı madde varsa atıf **yanlış `DOGRULANDI`** alır — 16 kanunun **7'si** bu yolla yanlış kanuna çözülüp `DOGRULANDI` aldı. Bu alet yayımlanan **`0/114`** manşetini üretendir; manşet **temiz çıktı ama TESADÜFEN** — 114 atfın hiçbiri parantezli adlı kanuna denk gelmemişti, **korunma aletten değil örneklemden** geliyordu. ✅ **ONARILDI 2026-09-11** (#67): sentetik prob `0/16 → 7/16` doğru, yanlış-`DOGRULANDI` `7/16 → 0/16`; çıpalar **oynamadı** (114→114, donmuş TEST **0/52 → 0/52** — o `0/52` de aynı aletle üretilmişti ve onarımdan önce **ayrıca ölçülmemişti**) ⇒ **artık korunma aletten geliyor** | Ad çözümünde **en çok TEK gevşemeye** izin verilir (≥2 sözcüklü sonek **ya da** baştaki fazla sözcükler — **ikisi birden asla**); ikisi birden gerekiyorsa **`AYRISTIRILAMADI`**, sessizce kanun **seçilmez**. `Hukum` çözülen kanunun **adını taşır** ki yanlış çözüm gözle görülsün | **#66** · **#67** |

## 2. Hakem / skorlama

| # | tuzak | ne olur | korunma | kaynak |
| :-- | :--- | :--- | :--- | :-- |
| 2.1 | **Red-regex kalibre edilmemiş** | Base'in baskın kalıbı `bulunmuyor`u görmüyordu → **M3 0,000** (gerçek 1,000), **M2b 0,662** (gerçek 0,987) | her **aile** için ayrı kalibrasyon; 15/15 ileri + 2/2 geri elle | #39 B3 |
| 2.2 | **Rakip ailesi kalibre edilmemiş** | Rakibin reddi **eksik sayılır** → sapma **bizim lehimize** | kalibrasyonsuz hiçbir çekinme sayısı raporlanmaz; `compare_runs.py` **⚠️ ile işaretler** | sprint1 CP7 |
| 2.3 | **A1 yerine ham ortalama** | Çekinme faith=0 alıp ortalamayı çeker: 12B'de ham 0,737 ↔ gerçek **0,904**; bu hatta base ALL 0,8385 ↔ **A1 0,973** | A1 **cevaplanan-only** (`rescore_answered.py`) + **coverage yanında** | ADR-0011 · #39 |
| 2.4 | **Coverage değişince A1 kıyası** | Model daha çok cevaplayınca A1 kıyası elmayla-armut (base kendi seçtiği kolay dilimde ölçülür) | **eşleştirilmiş alt küme**: ikisinin de cevapladığı sorular | #41 §4.2 |
| 2.5 | **`rejection_exact` ↔ `rejection_rate` karıştırılır** | regex-red ile LLM-red farklı şey ölçer; hakem "reddedip sonra açıklayan"ı ABSTAIN sayar, regex saymaz | ikisi **birlikte** raporlanır | #39 CP2 |
| 2.6 | **Payda kayar** | `valid_traps` modele göre değişir (M2b: 75/67/80) — oran aynı paydada değil | ~~oran payda ile yazılır~~ → **YETERSİZ, bkz. 2.14**: paydayı yazmak kirliliği **görünür kılar, gidermez** | #41 §3 · **#45** |
| 2.7 | **Sağlayıcı pinlenmemiş** | Aynı model kimliği farklı servis yığınında (farklı kuantizasyon) koşar → sayılar kıyaslanamaz, **hata vermez**. Tekrarladı 2026-09-13: ürün yolu kütlesi `["Azure","OpenAI"]`, çıpa `["OpenAI"]` ⇒ **−2,19 p'nin kaynağı ayrıştırılamadı** | özet JSON'da `judge_providers` **tek eleman**; yığın uyuşmazlığında `compare_runs.py` **tabloyu basmaz** | ADR-0029 · **#70** |
| 2.8 | **Hakem gürültüsü sinyal sanılır** | Aynı girdi + aynı hakem, iki koşu: `faith_macro` ±0,005 ama **`cit_precision` ±0,044** | `cit_precision`'da 0,04'lük fark **sinyal değildir**; `runs=1` künyeye yazılır | #41 §6 |
| 2.9 | **Regex kopyası çoğalır** | `rescore_answered.py` kalibre edilmemiş bir regex **kopyası** taşıyordu → tüm A1/coverage sayıları yanlıştı | tek kaynaktan import (`score_abstention.REJECT_RE`) | CP7 |
| 2.10 | **Eğitim biçimi hakemde cezalanır** | RAFT şablonunun 1. adımı (*"ilgili kaynak KAYNAK 3'tür"*) kaynak **hakkında** cümle → hakem `NOT_IN_SOURCE` der; `τ_g`'nin desteksiz iddialarının **%58'i** bu | 🔴 **AÇIK** — `τ_a`'dan önce karara bağlanmalı *(taşıyıcı `open_questions.md` §13.8 silindi)* | #41 §4.3 |
| 2.11 | **`.env` yüklenmez** | Hakem anahtarı görünmez, script ilk adımda durur (bu sefer **gürültülü** — para harcanmadı) | skorlama kabuğunda `set -a; source .env; set +a` | #41 |
| 2.12 | **Red-regex MOD'a göre yanlış** | Kör modun sistem istemi feragat cümlesini (*"bir avukata danışın"*) **emrediyor**; regex onu red sayınca dolu cevaplar çekinme oldu → ezber kütlesi **3,4× küçük** ölçüldü. M5 anti-hedef olduğu için sapma **lehimize**. base 54/56 · Gemini 58/62 · `τ_g` 50/51 "red" yalnızca o cümleydi | kalibrasyon **aile × MOD** — `exact_reject(cevap, mode)`, `mode` zorunlu (ADR-0044) | #43 B1 |
| 2.13 | **Tek eksende ölçüp "iyileşti" demek** | M2'de doğru davranış reddetmek → *"her şeye reddet"* diyen model **1,0** alır. Yeterlilik önsözü M2'yi 0,968'e çıkardı, M1 kütlesini **28,2**'ye düşürdü (aşırı-red 0,6875) | çekinme ekseninde her kazanç **karşı eksende** (M1 coverage/kütle) doğrulanır | #43 B5-b |
| 2.14 | **Filtre etiketi özne başına yeniden yargılanır** | 2.6'nın kökü. `valid_trap` hakemden **öznenin cevabıyla aynı çağrıda** isteniyor → hakem cevaba **çapalanıyor**, etiket kalemin değil **öznenin** özelliği oluyor. Aynı 80 M3 kaleminde 54/56/**39**; **19 kalemde** etiket özneye göre değişiyor; cevabı görmek `gpt-4o-mini`'yi **22,3 p** kaydırıyor (`gpt-4o` 8,4 p). Sapma **yön değiştirir** (M2b −7 p, M3 +12 p) — ön-kayıtlı kapı için gürültünün **en kötü** türü | tuzak geçerliliği **kalem düzeyinde, bir kez, cevaba KÖR** hesaplanıp önbelleğe alınır; özne skorlaması onu **okur**. Kontrol: `len({valid_trap})==1` | **#45** |
| 2.15 | **Tanım gereği bilinen etiketi hakeme sormak** | M3'te bağlam **boş** → "kaynak cevaplıyor mu" sorusunun cevabı 80/80 **hayır**; hakeme sorulduğu için 39-56 çıktı: gereksiz gürültü **ve** para | mod tanımından çıkan etiket **sabitlenir**, hakeme gitmez | **#45** |
| 2.16 | **A1 diye ham makro raporlanır — 2.3'ün YENİ betikte tekrarı** | `harness_tablo.py` `A1_tum` adıyla **puanlanan tüm kalemlerin** makrosunu yazıyordu; A1 **cevaplanan-only**. Çekinmeler de hakemden puan alıyor: k=5'te **0,9091** alıp makroyu yukarı, k=10'da **0,6819** alıp aşağı çekti — sapma **sabit kayma değil, yön değiştiriyor**. Bedeli: ön-kayıtlı kapının hükmü **tersine döndü** (bozukla %58,4 < %58,7 RET; düzeltilmişle %59,5 > %56,9 KABUL). ON/OFF kıyası da elmayla armuttu. Sayı **sekiz belgeye** yayıldı (`MODEL_CARD` dâhil) | A1 üreten **her** yol tek kaynaktan; `rescore_answered.py` ile **çapraz doğrula**. `A1` ve `faith_macro_tum` **ayrı alanlar**. **Türetilmiş metriğin adı onu üreten FİLTREYİ taşımalı** — `A1_tum` gibi belirsiz ad tam bu hatayı davet eder | **#54** |
| **2.17a** | ⭐ **Farklı sınavlar AYNI payda kaydını paylaşır** (önbellek anahtarı) | 2.14'ün tamamlayıcısı. İçerik-adresli önbellek anahtarı kaynak metnini **kliplenmiş** hâlde hash'liyordu: `h2b k=4` ile `k=10` 80 kalemin **15'inde** tek anahtara düştü (`id=12`: 4.461 ↔ 10.281 karakter, kaynak **4 ↔ 10**, ilk 3.500 aynı). k=10 kolu kendi paydasını **hiç ödemedi** ve özet dosyası `gecerlilik_devralinan: 15` diye **gururla** yazdı. 🚨 **REÇETE 2026-08-06'DA TERSİNE ÇEVRİLDİ (KARAR-4 m.1) — TEŞHİS ayakta, ÇARE değişti** | ~~anahtar tam kaynak metni üzerinde~~ → anahtar **hakemin GÖRDÜĞÜ metin** üzerinde (`sha256(soru ‖ kaynak[:SOURCE_CLIP])`). Gerekçe: tam-metin anahtarı **daha büyük** bir tuzak açıyordu — 5.363 istemin 65'i >1 anahtara, **83 garantili gereksiz çağrı**, `Rej*` ~1,5 p saf gürültü oynar ve *"k'nın çekinme bedeli"* diye okunur; üstelik onarımın sayısal karşılığı **yoktu** (15 çift yeniden ödendi, 15/15 aynı hüküm). **Gerçek çare klibi büyütmektir** (≈**$0,30**); ödenene kadar `k=10`'un paydası **TANIMSIZ** damgası taşır (ADR-0057), hüküm kurulmaz | **#58** |
| **2.17b** | **Ön-kayıtlı kapı maddesi ÇIPASIZ kalır** *(numara çakışması — bkz. üst kutu)* | Madde kurala uygun, sonuç görülmeden yazılır — ama karşılaştıracağı sayı **hiç ölçülmemiştir**. Isırdı 2026-09-07: ADR-0064 m.(3) *"M5 ≤ bugünkü"* diyordu, `tgta_v1`'in M5'i **hiçbir birimde ölçülmemişti**. Madde kendi kendine referans veriyordu, **hiçbir hüküm üretemezdi**, ön-kayıt kuralına uyduğu için gözden kaçıyordu. İkinci katman: çıpanın sayısı bulunsa bile **eski birimde** olabilir (v1 soru seti + 1024 bütçe) ve kıyas sessizce geçersizleşir | Kapı maddesi yazılırken çıpanın **dosya adı ve değeri** maddenin yanına yazılır — yoksa madde **eksik**, ön-kayıt tamamlanmaz. Tek soru: ***"bu sayıyı neyle, hangi birimde kıyaslayacağım?"*** Çıpa eski birimdeyse ADR-0050: **alet değişirse eşik aynı formülle yeni birimde yeniden türetilir** | **#62** · [ADR-0039](../adr/kararlar-0027-0044.md#adr-0039) · [ADR-0064](../adr/kararlar-0064-0085.md#adr-0064) |
| 2.18 | **Aynı ölçümün İKİNCİ bir aleti kalır** | 2.9'un veri tarafı. `valid_trap_cache.py` + `rescore_abstention_cached.py`, `score_abstention.py`'den **ayrı** bir kör payda üretiyordu ve **klipleri farklıydı**: aynı koşularda iki alet **1,2-2,2 puan** farklı sayı verdi (gürültü tabanının 4-7 katı). Kök sebep: 900 **parça** klipi **birleşime** uygulanmış — hakem 320 kaynağın **147**'sini görüyordu (%46). ⚠️ Tekrarladı 2026-09-11 (#67): `araclar.py` kendi **ikinci ad indeksini** taşıyordu ⇒ vatandaşa giden atıf doğrulaması ile yayımlanan sayıyı üreten doğrulama **ayrıydı** (892 adda 19, çıpanın atıflarında **24/114** ayrışma) | ölçümün tek kaynağı ana modül; yan betikler **ithal eder**, kendi klip/anahtar/hakem çağrısını taşımaz. **Bir sabitin adı hangi nesneye uygulandığını söylemeli** (`SOURCE_CLIP` ≠ parça klipi) | **#58** · **#67** |
| **2.19** | **Yayımlanan bağlayıcı sayının türetmesi YALNIZ DÜZ METİNDE duruyor** | `v1.0` kapısının çıpası `0,7425` **dört belgede** geçiyordu ama onu üreten bir betik ya da JSON alanı **hiçbir yerde yoktu** — sayı doğru çıktı, ama doğruluğu ancak ham puanlardan **yeniden kurularak** bilindi. İkinci katman aynı yeniden kuruşta çıktı: dedektör **13** kalemi çekinme saydı, hakem yalnız **8**'ini puanlamadı; aradaki bir kalem yanlışlıkla eklenseydi çıpa `0,7425 → 0,7488` olur, eşik **sessizce** yükselirdi | Bağlayıcı sayı yayımlanırken onu üreten **formül + kaynak dosya + kalem listesi** aynı belgede yazılır; yeniden-kuruş kanıtı olmayan sayı **doğrulanmamış** sayılır ve yayımlanmadan önce **en az bir kez sıfırdan türetilir** | **#70** (BULGU-A) |
| **2.20** | **GÖZ-katı düzeltmesi MİRAS ALINMAZ — miras alınan KÜMEDİR, puanlar değil** | İkinci hakem aynı 80 cevabı puanlarken **hangi kalemlerin çekinme sayıldığı** (id 15·24·27·28) miras alınabilir; **puanları** alınamaz — hakem değişince kalemin notu da değişir (id15 `1,0→0,0` · id24 `1,0→0,5` · id27 `1,0→0,0`). Puanlar da miras alınsaydı çıpa `0,6183 → 0,6496`'ya şişer, marj `+9,57 → +6,44 p`'ye inerdi — bu kez **lehimize** çalışırdı ama yine hata olurdu | Düzeltme kümesi taşınırken yalnız **üyelik** taşınır; her kalemin puanı **o koşunun kendi hakeminden** yeniden alınır. Ön-kayıt bunu **koşudan önce** yazar; **bir seferlik lehte sonuç kuralı haklı çıkarmaz** | **#70** (BULGU-I) |
| **2.21** | **KOŞULLU ölçütün koşulu ADINDA yok — aynı ad iki kapsamda** | *Aşırı-red* koşulludur: **altın geldi ve model yine de sustu**. Kalibrasyon dosyaları buna *"temiz çekinme"* dedi ve ad koşulu taşımadığı için **iki kapsamda** kullanıldı: üç Gemini kolunda yalnız altın-gelmiş kalemler (8+9+11 = **28**), bizim ve Sonnet-5 kolunda **tüm** çekinmeler (5 ve 8). Sonuç: Sonnet-5'in aşırı-redi **4 yazıldı, doğrusu 3**'tü; kart *"iki model eşit"* diyordu, oysa **rakip öndeydi**. Düzeltmeyi mümkün kılan şerh kalibrasyon dosyasının gerekçe metninde **zaten yazılıydı** ama **satıra hiç yansımamıştı**; eksen dört belgeye **dört ayrı sayıyla** yazıldı | Koşullu ölçüt **koşulunu adında taşır** (*"altın geldi & sustu"*). Kalibrasyon tablosu **okunan kalem sayısını ayrı sütun** olarak yazar. **Bir dosyanın gerekçe metnindeki şerh, satıra yansımadıysa YOK HÜKMÜNDEDİR** — şerh sayıyı değiştiriyorsa sayı o anda değiştirilir | **#71** |

## 3. Eğitim

| # | tuzak | ne olur | korunma | kaynak |
| :-- | :--- | :--- | :--- | :-- |
| 3.1 | **`--target-modules` verilmez** | Eski varsayılan `in_proj_*`'ı kaçırıyordu → **24 linear-attention katmanı LoRA'sız**, hata vermeden. Bedeli **`‖τ_g‖`'nin %26,8'i** | bayrak **zorunlu ve varsayılansız** | #40 B4 · #41 |
| 3.2 | **`all-linear` VLM'de** | Qwen3.5 bir **VLM** — `all-linear` görüntü kulesine de LoRA takar | metin kulesi modül listesi açıkça verilir (11 modül) | #39 CP0.7 |
| 3.3 | **Turn işareti yanlış** | `train_on_responses_only` **hiçbir şeyi maskelemez**, loss tüm diziden akar, eğitim **sessizce** bozulur | render'a karşı assert; **assert'in tetiklendiğini smoke'ta gör** | 12B #38 |
| 3.4 | **`fla-core` eksik** | `import fla` çalışır (→ fast-path **açık sanılır**) ama `fla.modules` yok → **model hiç yüklenmez**. Eksik `fla-core`, fla'nın hiç olmamasından **kötü** | `--no-deps` kurulumda `fla-core` ayrıca | #40 B1 |
| 3.5 | **`causal-conv1d` yok** | *"fast path is not available"* uyarısı — **uyarı ölçüt değil**, `s/it` ölçüt; fallback matematiksel olarak aynı | hızı ölç, uyarıyı okuma | #40 · #41 |
| 3.6 | **Modal yol tuzağı** | `--data` **konteyner** yoludur; yerel yol verilirse koşu yanar | `/data/<dizin>`; script model yüklenmeden **saniyede** patlar | #40 B4 |
| 3.7 | **`remote()` / `--detach`siz `spawn`** | Efemer app entrypoint bitince kapanır, job **iptal olur**; `remote()` client'a bağlı bekler, WSL kapanınca job ölür. **4 koşu yakarak** öğrenildi | `spawn()` + `--detach` | 12B dersler |
| 3.8 | **ORPO `--adapter` (continuation)** | `--adapter tg` **ardışık SFT** üretir — kolu değil **Taban B'yi** kaydeder, fark edilmez | `--fresh-adapter` **zorunlu** | TASARIM §4.1.1 |
| 3.9 | **Rejim sapması** | precision / `r`/`alpha` / dropout / seed / `target_modules` kollar arasında ayrışırsa `τ = θ_ft − θ_base` bozulur → **merge geçersiz** | zorunlu-eşleşen tablosu | ADR-0036 |
| 3.10 | **Epoch sayısı 1 sanılır** | 1.741 çift ÷ etkin batch 64 = epoch başına **27 adım**; `τ_g` 1.083 adım koşuyor. 1 epoch ORPO ham base'den yeni davranış öğretmez | ORPO **`epochs = 3`** (82 adım) | TASARIM §4.1.1 |

## 4. Veri

| # | tuzak | ne olur | korunma | kaynak |
| :-- | :--- | :--- | :--- | :-- |
| 4.1 | **`--exclude`suz DEV üretmek** | CORE-HARD seçimi **seed'den bağımsız ve sıralı** — farklı seed aynı maddeleri verir; `--exclude`suz "DEV" TEST'in **ikinci kopyasıdır**, hata vermez | `--exclude` + **kesişim = 0** kabul ölçütü | sprint1 CP1 |
| 4.2 | **Teacher jargonu sızar** | Eğitim hedeflerinin **%7,51'i** teacher'ın iç etiketini (`GOLD`/`DISTRACTOR`) taşıyordu; öğrenci o etiketi girdide **hiç görmediği hâlde** ezberler | `scrub_teacher_jargon.py`; teacher **öğrencinin gördüğü etiket uzayında** promptlanır | #39 B4 |
| 4.3 | **Truncation** | `max_seq_len` yetmezse örnek "tüm label −100" diye düşer ya da cevap **ortadan kesilir** — *yarım cevap öğretimi*, uyarısız. Suçlu cevap değil **kaynak bloğu** | `measure_token_budget.py`; **etkilenen >%1 ise koşma** | 12B #15 |
| 4.4 | **Başka modelin hataları** | `orpo_abstain/`'in `rejected` tarafı **12B'nin fabrikasyonları** — yeni modele **başka bir modelin hatalarını** öğretir | yeni base ile **yeniden hasat** | TASARIM §4.1 |
| 4.5 | **Topik-skew** | Seed dosyası kanuna göre **sıralı**ydı; yarıda kesilen üretim "rastgele örnek" değil **sistematik kapsama deliği** verdi | üretim öncesi shuffle + kapsama kontrolü | 12B #14 |
| 4.6 | **Veri şeması sessizce yanlış** | Pack `source` alanını gold metni sanıyordu — o alan **provenance etiketiydi**. Hata vermedi; 16 çağrılık smoke yakaladı | her veri üretiminden sonra **küçük smoke** | 12B #14 |
| 4.7 | **Kabul ölçütü ≠ raporlanan metrik** | ORPO `rejected` hasadının ölçütü **regex**, tabloların okuduğu sayı **LLM hakemi**. *"Reddedip sonra açıklayan"* cevabı regex fabrikasyon sanıyor → havuza **doğru çekinme örnekleri** `rejected` olarak giriyor → ORPO modele **çekinmeyi cezalandırmayı** öğretir. Regex %30 kabul dedi, hakem geçerli tuzaklarda **%71'ine ABSTAIN** dedi; gerçek verim **%5**. Eğitim koşar, loss düşer, hiçbir yerde hata yok | veri hasadının kabul ölçütü, o veriyle eğitilen kolun **raporlanacağı metrikle aynı** olmalı; regex↔LLM farkı (CP0.9: **0,61 ↔ 0,814**) **ölçülüp** ölçüte bağlanır | **#44** |
| 4.8 | **Tuzak havuzunun kendisi geçersiz** | ⚠️ **BÜYÜKLÜĞÜ ÇÜRÜDÜ (#45)** — "%42" zayıf hakemin **çapalanma artefaktıydı** (bkz. 2.14); `gpt-4o` aynı 36 kalemde **%8,3-16,7** diyor. **Olgu duruyor, oran durmuyor.** Buna dayanan ön-eleme filtresi **net zararlı** çıktı (kestiği 7 kalemin 6'sı geçerli tuzak) → koşulmadı | `valid_traps` hasat hunisinin **ayrı bir satırı** olarak raporlanır; **ölçen hakem cevaba kör olmalı ve gücü rapor edilmeli** | #44 · **#45** |

## 5. Artefakt / araç

| # | tuzak | ne olur | korunma | kaynak |
| :-- | :--- | :--- | :--- | :-- |
| 5.1 | **Kuantizasyon yarıda kesilir** | Dosya "var" görünür — bir GGUF 666 tensörün 4'ünde kesilmişti, **70 MB** diskte duruyordu | `setup_llamacpp.sh` <100 MB'da durur; boyutu **base ile karşılaştır** | sprint1 CP0 |
| 5.2 | **Merge sessizce eksik** | Adaptörün bir kısmı base'de karşılık bulamazsa dosya yine yazılır ve *"merge oldu"* görünür | **uygulanan/toplam** eşitliğini assert et (224/224) | #41 §2 |
| 5.3 | **Merge doğrulanmaz** | Yanlış merge **normal boyutta, normal görünümlü** bir model üretir | iki kapı: `‖merged − base‖_F ≈ ‖τ‖` **ve** GGUF **base ile aynı boyutta** | #41 §2 |
| 5.4 | **`set -e` mesajı yutar** | `snap=$(ls …)` gibi bir atama başarısız olunca `set -e` **mesaj basmadan** öldürür; script "sessizce" durur | beklenen başarısızlıklarda `\|\| true` + açık kontrol | #41 |
| 5.5 | **Göreli yol `cd`'den sonra** | Script içinde `cd` varsa argümandaki göreli yol **başka yere** çözülür | argümanları **başta mutlaklaştır** | #41 |
| 5.6 | **`.env` emekli base'i gösterir** | `BASE_MODEL` eski hattı işaret ediyordu — ADR-0026'nın tam uyardığı tuzak; koşular `--model` zorunlu olduğu için kurtuldu (**şans, tasarım değil**) | base **parametre**, varsayılan **yok**, tanımsızsa **erken patla** | #39 B5 |
| 5.7 | **Ara f16 GGUF diski doldurur** | 8-22 GiB; `KEEP_F16=1` unutulursa birikir | varsayılan sil; kalıcı iş `~/code/` altına | sprint1 CP0 |
| **5.8a** | **Paylaşılan modülleri alt klasöre taşımak uzantısız import'ları SESSİZCE kırar** | Düz yığında `import runlock` çalışır (Python dosyanın **kendi dizinini** `sys.path[0]`'a koyar); alt klasöre bölününce kardeş modül kaybolur ⇒ **`ImportError` çalışma zamanında**, bazen bir GPU koşusunun ortasında. ⚠️ İkinci katman: `pytest` bunu yakalar ama **alt süreç çağrılarını yakalamaz** — 6 kırığın altısı da `subprocess([sys.executable, "scripts/X.py"])` biçimindeydi. Üçüncü katman: envanter 18 dosyada `sys.path.insert` saydı, kardeş import eden **26** dosya vardı | (1) Taşımadan **ÖNCE ve ayrı commit'te** yol köprüsü kurulur ve `pytest` o adımdan sonra **hâlâ yeşil olmalı**, değilse taşımaya geçilmez (Tidy First). (2) Köprü gerekenler `sys.path.insert` **aranarak değil**, `ast` ile **kardeş import taranarak** bulunur. (3) Taşıma sonrası `verify:` **döngüyle**: her `scripts/...py\|sh` string'i var olan dosyayı göstermeli — gözle değil. (4) `pytest` yetmez, **uçtan uca duman koşusu** şart | **#62** |
| **5.8b** | **WSL2 belleği host'un yarısı** *(numara çakışması — bkz. üst kutu)* | Host 32 GB olsa da VM **15,9 GB** görür (`.wslconfig`'de `memory=` yoksa) | bellek hesabını **`free`'den** yap, host'tan değil | #41 §2 |
| **5.9a** | **Denetim için `git stash`, sonra repo DIŞINDAN `stash pop`** | Pop **yanlış çalışma dizininde** koşarsa `fatal: not a git repository` verir ve **değişiklikler stash'te kalır**. Isırdı 2026-09-07: pop `/tmp`'den denendi; aynı turda **takipli bir dosya çalışma ağacından kayboldu** (18 KB, `git restore` ile kurtarıldı, hiçbir commit'e girmemişti). ⚠️ Zarar sınırlı kaldı **yalnızca dosya takipli olduğu için** — takipsiz bir dosya **kalıcı kaybolurdu** | Denetim koşusu için **stash KULLANMA**: ayrı bir `git worktree` ya da geçici klon. Zorunluysa `stash`/`pop` **aynı komut zincirinde ve repo kökünde**, ardından `git status` ile **silinen (`D`) dosya** denetlenir. ***"Önceden de var mıydı"* sorusu çalışma ağacını riske atmadan cevaplanabilir** | 2026-09-07 |
| **5.9b** | **`pgrep`/`pkill -f` kendini yakalar** *(numara çakışması)* | `pkill -f "llama-server -m models/..."` kendi komut satırıyla eşleşip **kabuğu öldürdü** (exit 144); `until ! pgrep -f "…"` döngüsü kendini görüp **asla çıkmadı** (13 dk boşa) | desende köşeli parantez: `pkill -f "[l]lama-server"` | #43 |
| **5.10** | **Koşu YAZARKEN çıktı dosyasını commit'lemek** | `git add -A` bir üretim koşusunun ortasında çalışırsa **yarım** `detail.jsonl` depoya girer ve orada **kalır**; dosya geçerli JSONL, yalnız **eksik** — hata yok, uyarı yok. Isırdı 2026-09-07'de **iki kez** (biri 28/80 satır commit'e girdi ve ancak bir denetimle görüldü) ve **2026-09-12'de yine iki kez** (#70) ⇒ **aynı ders üçüncü kez** | Uzun koşu sürerken **`git add -A` yapma**; gerekiyorsa koşunun yazdığı dizini açıkça hariç tut ya da bekle. **Alt ajan koşarken dosyalar adıyla eklenir, `outputs/eval/**`'e koordinatör hiç dokunmaz.** ⭐ Bedava kontrol: yarım ile tam dosyanın **ortak kalemleri bayt-bayt aynı** olmalı — 28/28 tuttu ve bu rejimin **deterministik** olduğunun kanıtı oldu | **#63** · **#70** |

## 6. Bulut / orkestrasyon

| # | tuzak | ne olur | korunma | kaynak |
| :-- | :--- | :--- | :--- | :-- |
| 6.1 | **`modal run` efemer app + `spawn()` = iş HİÇ koşmaz** | Terminal *"✓ App completed"* yazar, log yalnızca *"Stopping app"* içerir. **Hiçbir iş başlamamıştır, hata da yoktur** — yerel giriş noktası dönünce efemer app kapanır ve kuyruktaki iş onunla ölür | **`modal run --detach`** zorunlu; *"SPAWNED ✓"* işin koştuğunu **kanıtlamaz** — `modal volume ls <out>` ile çıktının yazıldığını doğrula | **#47** |
| 6.2 | **Veri kapısı model yüklemeden SONRA** | Yol/format hatası model A100'e yüklendikten sonra patlar → boşa GPU dakikası. `train_orpo.py --data` bir **dizin** bekliyordu; `train_sft.py` aynı denetimi **yükleme öncesi** yapıyordu | her eğitim betiği girdiyi `os.path.isfile` ile **model yüklenmeden önce** doğrular | **#47** |
| 6.3 | **Bütçe defteri iki cüzdanı toplar** | Defter *"Modal cap $42,50 · harcanan $6,57 · kalan $35,93"* diyordu, gerçek Modal kalanı **$7,27**'ydi: harcamaların neredeyse tamamı **OpenAI hakem** ücretiydi, ~$28'lik Modal harcaması **emekli 12B hattına** aitti ve bu defterde hiç görünmüyordu → CP4-CP5 sığmaz hâle gelmişti ve fark edilmemişti | GPU ve hakem maliyetleri **ayrı tabloda**; Modal sayısı **panelden okunur**, defterden türetilmez | **#47** |
| 6.4 | **Hazır imajın `ENTRYPOINT`'i** | llama.cpp imajının entrypoint'i `/app/llama-server`; temizlenmezse çalıştırıcının `python …` komutu **ona argüman olur**. Belirti: konteyner **saniyeler içinde** ölür, GPU logu hiç açılmaz — sebebi kodda değil **imajda** | üçüncü-taraf imajda `.entrypoint([])` ile **açıkça temizle**; ilk koşuda konteyner logunun ilk 5 satırını gözle oku | **#48** |
| 6.5 | **Lock dosyası iş türüne göredir** | `requirements.lock.txt` **eğitim** için üretildi; hasat/eval bağımlılığı (`openai`) içinde **yok** → `ModuleNotFoundError` konteynerde, **GPU ayrıldıktan sonra** patlar, para yanar | yeni **iş türü** = bağımlılıkları imaj katmanında **açıkça** ekle (`pip_install("openai==2.41.0")`, sürüm yerelle eşitlenir); lock'un adı hangi işe ait olduğunu söylemiyorsa söylet | **#48** |
| 6.6 | **Öbekli eş zamanlı istemci slotları boş bekletir** | `pool.map`/chunked desende **her öbek en yavaş kalemini bekler**; hata yok, çıktı doğru — sadece **%30 pahalı**. Slot doluluğu **%70** (**10.220** slot-s iş ÷ **14.496** slot-s kapasite); sürekli beslemeye geçince **1,84×** hızlanma (3,65 → 1,98 s/üretim) | kalemler **bağımsızsa bariyer kullanma**; istek/sıra/seed değişmediği sürece bu **yalnız zamanlama** değişikliğidir | **#48** |
| 6.7 | **`gpu="A100"` takma adı iki farklı kart verir** | Etiket hem **40GB** hem **80GB** kart verebiliyor; bant genişliği **1,55 ↔ 2,03 TB/s = 1,31×**. Künyeye gerçek kart yazılmazsa hız farkı **yanlış sebebe** yazılır — `-np 32` ↔ `-np 64` kıyasında gözlenen **1,32×**, *"slot artışı işe yaramadı"* ile *"np64 yavaş karta düştü"* **ayrılamadı** | künyeye `nvidia-smi`'den okunan **gerçek kart adı** yazılır; `gpu_gercek` eşit değilse kıyas **kurulmaz** | **#48** |
| 6.8 | **Kümülatif ortalamayı erken okuyup karar verme** | Yüksek eş zamanlılıkta **ilk dalga inmeden** `süre ÷ tamamlanan` şişik görünür: `-np 64` koşusu 75. üretimde **4,96 s/üretim** gösterdi, **2,93**'te kapandı. Ara çıktıya bakıp iptal etmek sağlıklı koşuyu öldürür ve parayı **iki kez** yakar | verim kapıları `gate_after_s` ile **gecikmeli** bakar (ADR-0047 m.3); hiç değerlendirilmediyse künyeye `değerlendirilmedi` yazılır — *"geçildi"* damgası **hak edilmeden basılmaz** | **#48** |
| 6.9 | **Ön-kayıtlı bir kapıyı kümülatif ortalamayla beslemek** | **6.8'in kod tarafındaki kardeşi** — orada *insan*, burada *kapı* aynı yanlı sayıyla **otomatik** karar veriyor. Açılış geçicisi her kaleme paylaştırılınca hız **sistematik olarak kötü** görünür ve kapı, eşiği **geçecek** bir koşuyu durdurur: gerçek kararlı hızı ~**2,4 s/üretim** olan koşu 600. saniyede **2,97** okunup durduruldu (eşik **2,88**) — **aynı koşu 73 sn sonra zaten 2,89'daydı**. İki tip durduruldu, ~$1,2 GPU yandı; künyeye düzgün bir 🔴 damga bile düştü | kapı **kararlı hızı** ölçsün (boru hattı `concurrency` kalemle **dolduktan sonrası**); kümülatif alan **silinmez, yanına yazılır**. **Kural: sonucu gördükten sonra EŞİK değil ALET düzeltilir** — ön-kayıtlı olan *büyüklük*tür, onu kestiren tahmin edici değil; ayrım raporda korunmazsa dışarıdan **gevşetme** görünür | **#48** · [ADR-0050](../adr/kararlar-0045-0063.md#adr-0050) |
| 6.10 | **İki detached iş aynı çıktı dizinine yazar** | `--detach` işi terminalden koparır; ikinci başlatma birinciyi durdurmaz, **yanına** koşar. Aynı `--out`'a yazınca: GPU maliyeti **iki katı** · havuz sırası deterministik olduğu için **yinelenen id** · `KUNYE.json`'u **son biten iş yeniden yazar** ⇒ künyedeki sayaçlar dosyayı tarif etmez · yanlış olanı durdurunca **yarım son satır**. Devam mekanizması korumaz: her iş **kendi başlangıcındaki** id kümesini okur | başlatmadan **önce** `modal app list \| grep <fn>`. Çakışmada **ilerlemesi fazla olanı** tut (başlangıç saatini değil). Çıktı kabul/eğitime girmeden **id bazlı tekilleştirme + yarım son satır onarımı** zorunlu; **ortadaki** bozuk satır onarılmaz, çökertir | **#48** §5 |
| 6.11 | **Devam mekanizması yalnız KABUL edilenleri hatırlar** | Elenenler yalnız sayaçta kalır ⇒ ek tur iki yoldan da sessizce boşa gider: *taze dizinde* sıra **0'dan** başlar ve **aynı kalemler** yeniden üretilir; *aynı dizinde* üretimlerin ~**%69'u** zaten elenmiş kalemlere gider. Hata yok, GPU yanar, sonra tekilleştirme havuzu küçültür ve azlık **yanlış sebebe** yazılır | devam kaydı *"denendi"*yi ifade edemiyorsa ek tur **sıra atlamayla** koşulur (`--skip-first N`, N = önceki künyedeki `denenen`); künyeye `skip_first` yazılır. **"Kaldığın yeri" saklamayan bir devam mekanizması, devam ettiğini SANMANA izin verir** | **#48** §9 |
| 6.12 | **Bayrak script'e eklenir, çağrı zincirine eklenmez** | Alt betiğe eklenen bayrak orkestratörde güncellenmezse komut satırındaki değer **sessizce yok sayılır**: `argparse` şikâyet etmez (bayrak alt betikte var), Modal şikâyet etmez (bilinmeyen kwarg üst katmanda hiç oluşmaz), künyeye de yazılmaz. Ek tur 1. turun kalemlerini **baştan üretti — 3 saat GPU, tamamı yinelenen**. Bu turda **dört kez** aynı sınıf çıktı | ekleme, **çağrı zinciri uçtan uca izlenmeden** tamamlanmış sayılmaz: alt betik imzası → orkestratör imzası → komut kurulumu → **künye alanı**. Doğrulama gözle değil **logdan**. **Künyeye yazılmayan bir parametre koşuldu sayılmaz** | **#48** §12 |

## 7. Erişim / harness

| # | tuzak | ne olur | korunma | kaynak |
| :-- | :--- | :--- | :--- | :-- |
| 7.1 | **Prob betiği sözleşmeden sapar, API'yi suçlu gösterir** | `bedesten_probe.py` kimliği `documentId`/`id` ile arıyordu; API'nin alanı **`mevzuatId`** ve sözleşme belgesi **en baştan doğruydu** ⇒ arama `SUCCESS` dönerken `id: None`, ardından `ERROR` — **API çalışırken prob "bozuk" raporluyor**. Bu yönde bir yanlış, güncellik iddiasını gereksiz yere düşürüp yanlış düzeltme yaptırırdı. Betik sözleşmenin 3. çağrısını **hiç sınamıyordu** | *"Sözleşme bozuk"* sonucu **ham yanıt gözle okunmadan** kayda geçmez; prob **her** çağrıyı kapsar. **Ölçüm aracının kendisi de sessizce yanlış olabilir — negatif sonuç, aracı aklamadan rapor edilmez** | **#49** |
| 7.2 | **Gömme modellerini farklı bağlam penceresinde kıyaslamak** | `sentence-transformers` her modelin **kendi** `max_seq_length`'ini kullanır (e5-base **512**, bge-m3 **8192**); sabitlenmezse *"hangi model daha iyi"* ile *"hangisi daha çok metin gördü"* karışır ve fark hiçbir yerde görünmez | `model.max_seq_length` **açıkça** eşitlenir ve künyeye yazılır; eval-ayna kırpmasıyla (900 kar) uyumlu seçilir | **#49** |
| 7.3 | **e5 ailesini önek olmadan koşmak** | `query: `/`passage: ` önekleriyle **eğitildi**; öneksiz kullanım çökmez, yalnız modeli **sistematik olarak düşük** gösterir — ve gömme seçimi tam bu sayıyla yapılıyor. bge-m3 ise önek **istemez**, ona eklemek de bozar | kart/README'deki **önek sözleşmesi** okunur ve koda aile bazında girer; künyeye hangi önekle koşulduğu yazılır | **#49** |
| 7.4 | **`recall@k`'yı erişim kabiliyeti sanmak** | `core_hard.jsonl` **madde verilmişken cevaplama** kümesi; sorular tek başına hangi kanuna ait olduğunu söylemiyor (*"Başvurum kabul edilirse ne olur?"*) ⇒ tek-altın `recall@k` düşük çıkar ve **retriever zayıflığı gibi okunur**. Havuz altının kendi kanunuyla sınırlanınca aynı BM25 **0,625 → 0,875** | erişim ölçümü soruların **kendi başına ayırt edici** olduğu kümede yapılır; olmuyorsa oracle-kapsam sayısı **yanında** raporlanır | **#49** |
| 7.5 | **Falsy sıfır ölçüm betiğini sessizce yanlışlar** | `(sira or 99) < k` deyiminde **`sira == 0` falsy** ⇒ **1. sıradaki her isabet kaçırılmış sayılır**. Temiz alt kümenin `recall@5`'i **0,2933** çıktı (gerçek **0,7467**) ve sayı *"korpus kirliliği recall'ı şişiriyor"* tezini **ters yönde doğrulamış** görünüyordu — **hata, aranan bulguyu destekleyen yönde yalan söyledi**. Yakalanma sebebi **aritmetik tutarsızlık** (22 + 5 ≠ 60), gözle okuma değil | `is None` ile açıkça karşılaştır. **Türetilen bir alt küme sayısı tüm küme sayısıyla toplanabilir olmalı — toplamıyorsa betik yanlıştır, bulgu değil** | **#52** |
| 7.6 | **Korpusun madde kimliği güvenilir sanılır** | `(kanun_no, madde_no)` anahtarının **%22,7'si yineleniyor** (9.192 fazladan satır). İki sessiz sebep: (a) **tablo parçalanması** — `3520`'nin 1.166 hücresi "madde" diye indekslenmiş; (b) **alt-madde soneki `madde_no`'ya değil metne düşüyor** — `2004/309` **27 kez** var, çünkü `309/a`, `309/b`… hepsi `Madde 309` etiketli. Sonucu: doğrulayıcı `309` ile `309/a`'yı **ayırt edemez**, `recall@k` yanlış eşleşmeyi **isabet** sayabilir, ve bir `DOGRULANDI` damgası **39 karakterlik yanlış ayrıştırılmış parçaya** basıldı | altın anahtarın korpusta **kaç kez geçtiği** yanında ölçülür; tekil-anahtar alt kümesi ayrı raporlanır (bu turda: 5/80 etkileniyor, `recall@5` 0,7500 ↔ temiz 0,7467 → **sapma yok**). **Korpus temizliği varsayılmaz, ölçülür** | **#52** |
| 7.7 | **Kapı, var olmayan alanı okuyup HER ZAMAN düşüyor** | `yeniden_uret.sh`'ın recall@10 kapısı `r.get("gold_retrieved")` okuyordu; detay kayıtlarında bu alanlar **hiç yok** ⇒ sayaç sessizce **0**, `recall@10` **her koşuda 0,0000**, kapı *"harness OYNAMIŞ"* diye **her zaman** düşüyordu — **çıpanın kendi verisinde bile** (gerçek 0,9500). Bu sınıf 7.1/7.5'ten **ters yönde**: onlar ölçümü iyi gösterir, bu **sağlıklı koşuyu sürekli suçlu** gösterir ve insanları kapıyı **gevşetmeye ya da yok saymaya** iter — **geçen bir kapıdan tehlikelidir**, çünkü hiç geçmeyen kapı "zaten hep kırmızı" diye göz ardı edilir. ✅ KAPANDI (kusur 33, `cc12b6e`) | `.get()` ile sessiz `None`/`False`'a düşen bir alan adı, o alanın **gerçekten kayıtta var olduğu** en az bir gerçek dosyada gözle doğrulanmadan kapıya yazılmaz. **Bir kapı hiçbir koşuda geçmiyorsa şüphe eşiğe değil ALETE düşer** (ADR-0050) | ADR-0083 (kusur 33) · **#70** |
| **7.8** | **Sağlık kontrolü BAŞKA bir sürecin sunucusuna 200 alıp "kendi sunucum hazır" sanıyor** | Port zaten **başka bir süreç** tarafından tutuluyorsa yeni `llama-server` `couldn't bind` ile **anında ölür**, ama `curl /health` döngüsü eski sürece `200 OK` aldığı için *"✅ hazır (pid …)"* yazıp devam eder — pid'i basılan süreç **zaten ölü**, istekler **tamamen başka bir modele** gidiyor. Hata yok, log yeşil, **8 kalem yanlış model karşısında üretildi**. ⚠️ Bu turda **bir yayımlanmış sayıyı da** vurabilirdi; o koşuda korunma **aletten değil TESADÜFTEN** geldi (port boştu). ✅ Korunma **koda yazıldı** (TDD, #70 §6): tuzak sahte `curl` ile **4/5 testte reprodüklendi**, sonra `dogrula_model_kimligi()` her iki dala bağlandı | Sunucu başlatıldıktan sonra **`/v1/models` ile hangi GGUF'un gerçekten servis edildiği doğrulanır** (ya da `kill -0 $SRV`) — health-check 200'ü *"benim sürecim ayakta"* ile eş tutmak **yeterli değildir**. Arka plan işi başlatmadan önce hedef portun **boş olduğu** kontrol edilir | S17 (§BULGU-E) · **#68** · **#70** |

---

## Kullanım — koşu öncesi kısa liste

**Eval:** `--data` açık mı · **`--thinking on` + `--think-budget 1024` + `--max-new-tokens 512`**
(ADR-0043; thinking-off canlı rejim **değil**) · `--max-chunk-chars 900` · n/seed CP2 ile aynı mı ·
taşıyıcı CP2 ile aynı mı · **koşu klasörü + `KUNYE.json`** yazıldı mı · kesik oranı %5'in altında mı ·
**`/v1/models` ile sunucunun hangi GGUF'u servis ettiği doğrulandı mı** (7.8).

**Eğitim:** `--target-modules` verildi mi · turn işareti assert'i **tetiklendi mi** · `fla-core` kurulu mu ·
`--data` konteyner yolu mu · `spawn()` + `--detach` · rejim §4.1.1 ile eşleşiyor mu.

**Skorlama:** `.env` yüklü mü · sağlayıcı pinli mi (2.7) · red-regex bu **aile VE bu mod** için kalibre mi
(ADR-0044) · A1 cevaplanan-only mu · coverage yanında mı · payda yazıldı mı · çekinme kazancı
**karşı eksende** doğrulandı mı · bağlayıcı sayının **türetmesi** aynı belgede mi (2.19).

**Artefakt:** merge 224/224 mü · `‖merged − base‖ ≈ ‖τ‖` mi · GGUF boyutu base ile aynı mı ·
kuantizasyon bitti mi.

**Para:** tahmin **puanlanacak öznenin kendi profilinden ve aynı moddan** mı türetildi (1.11) ·
gerçek fatura **×1,6 gateway marjı** hesaba katıldı mı.
